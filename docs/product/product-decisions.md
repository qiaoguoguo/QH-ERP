# 产品决策记录

> 阅读说明：本文按决策形成顺序保留历史口径。较早章节中的“当前”“一期不启用”等表述只代表该阶段当时边界；项目现状以本文最上方最新日期、当前交接和已完成阶段任务说明为准。

## 2026-07-21：033 项目利润与经营财务分析完成，待最终合入交付

- 交付状态：033 已完成实现、唯一集中审查、一次集中整改、差异复审和唯一交付前全量验证；产品、UI、后端差异复审均已通过，clean5 验证器 192/192，受影响真实桌面路径最终通过。当前仍等待主代理最终合入交付，本条不预写主分支哈希、远端同步状态或正式验收服务端口。
- 014 继承：033 是 014 经营报表基础的受控丰富，继续使用同一个 `/reports` 前端入口、`reporting` 后端领域、`/api/admin/reports` 只读 API、固定报表交互、`summary + items`、十进制字符串和来源追溯契约；不新建平行经营分析产品域、平行 API 前缀、平行权限家族或可配置 BI。
- 固定报表边界：033 新增项目利润、合同回款、采购差异、库存资金、往来账龄、经营/会计对照和固定经营财务摘要。项目利润以 029 发货经营收入为主口径，并列展示开票、目标和 031 `POSTED + PROJECT` 会计口径；无会计事实或不可对账时显示 `UNAVAILABLE`，不得汇成零利润或强行匹配。
- V35 与快照：V35 只前向追加 8 个查看权限、7 个菜单项、5 个 030 经营侧冻结报表代码和必要索引；五个可冻结报表为 `PROJECT_PROFIT`、`CONTRACT_COLLECTION`、`PROCUREMENT_VARIANCE`、`INVENTORY_CAPITAL`、`RECEIVABLE_PAYABLE`。经营/会计对照和固定经营财务摘要不进入 030 `BUSINESS_SNAPSHOT`，请求快照口径时返回 `REPORT_BASIS_INVALID`，不得以实时结果伪装冻结快照。
- 财务摘要与排除项：固定经营财务摘要只表达管理用收入、主营成本、期间费用、其他损益、所得税费用、经营结果、资产/负债/权益类别余额和试算/银行/税务基础状态，不使用“资产负债表”“利润表”“现金流量表”标题，不用于正式申报、税控、正式收入确认或法定财务报表。
- 数据边界：033 只读消费 023 至 032 已交付事实，不创建、修改、删除或回填库存、生产、销售、采购、往来、项目成本、030 快照、031 凭证账簿、032 财务关闭、银行或税务事实。029 的未定价人工红样本、030 的真实关闭和 033 的五类 `FROZEN` 快照通过演示数据期间隔离同时保留，不允许补零、降级差异、按项目状态硬跳过或回填快照。
- 下一阶段：034 平台交付治理完善尚未启动；不得把 033 扩展为法定财务报表、自由 BI、多组织、多账套、多币种、正式税务申报、税控、APS/MES 或工艺路线。

## 2026-07-21：032 财务结账与资金税务基础正式交付

- 交付状态：032 已完成唯一集中审查、一次集中整改、差异复审、最终差异复核、唯一交付前全量验证、产品文档同步、纯快进合入并推送 `main`；本地 `main`、`origin/main` 和实际远端均为 `c823861fd467b78119fb964a2f1d41314129186f`。正式 API `18080`、Web `5173` 均为 200。
- 领域边界：032 新增独立 `financialclose` 领域和 V34 数据模型，产品导航归现有“会计核算”和 `/gl` 路由族，不并入 030 `periodclose` 或 028 `finance`。030 业务月结 `CLOSED` 是财务关闭强前置，但 030 仍只表达业务月结与经营快照；031 提供正式凭证和总账，032 通过 031 草稿和审批记账承接会计影响。
- 财务结账：会计期间当前态只使用 `OPEN/CLOSED`；关闭前执行九项检查、强权限、原因、确认、期间锁、事务复检、幂等和审计，不叠加审批。反结账通过 022 `FINANCIAL_PERIOD_REOPEN` 双人审批，申请人不得自审，审批通过后期间恢复 `OPEN`；`REOPENED` 只作为关闭运行历史状态，不成为期间当前态。
- 损益、银行与税务：月末损益结转只把收入费用余额转入 `4103 本年利润` 并生成 031 正式凭证草稿，不做年末利润分配。银行能力包括账户、账号脱敏、CSV 预览/确认、显式候选、多对多对账、四类方向化未达、确认和重开。税务能力只包括档案、有效期税率/票种/科目、增值税汇总、附加税建议、企业所得税估算、确认/调整、税款缴纳与更正台账。
- 正式部署与验证：正式库已由 V33 自然前迁至 V34，V34 checksum 为 `-629066235`，V29—V33 checksum 保持 `774334682`、`2130342893`、`-2074547591`、`249406902`、`612501943`，失败迁移 0，完整验证器 170/170。数据库 `AVAILABLE` 文件与 `qherp-private` 本次为 18/18；该对象数只作本次正式事实，不写成长期固定契约。部署前备份为 `C:\Users\14567\.codex\backups\qherp\032-main-before-v34-20260721-043029`，dump 1118966 字节，bucket 18 个对象/44385 字节。
- 正式数据与排除项：030 原 `OPEN/BLOCKED`、3 阻断/2 警告且无关闭/重开/快照保持不变；031 与 032 正式业务事实均为 0，未为验收补造结账、凭证、银行或税务事实。032 不提供完整财务报表、033 经营财务分析、电子税务局申报、税控、发票查验/用途确认同步、完整红字发票、出口退税、多公司、多账簿或多币种。
- 桌面与后续：正式 12 个桌面入口均可访问并加载收口，控制台 error 0、页面级横向溢出 0；未截图、未测移动端。隔离 `18083/5175`、`qherp_032_review`、`qherp-032-review` 和临时目录已清理为 0。正式部署仅保留 `/gl/tax-settings` 少量原始枚举、沿用检查编码“-”、确认对账候选本地可勾选、tooltip、构建分块提示等一般项，不影响数据、权限、状态或关键动作。该条保留 032 交付时边界；033 当前状态以上方 033 决策为准。

## 2026-07-20：031 会计科目、凭证与总账基础正式交付

- 交付状态：031 已完成实现、集中审查整改、差异复审、唯一全量验证窗口、最终证据刷新、纯快进合入、正式 V33 部署和正式验证，业务节点 `15ab4020a765186c4e1e25fe98755bd4b0a1fb64` 已进入正式主线。
- 核算边界：新增独立 `gl` 领域，固定单公司、单账簿 `MAIN`、人民币 `CNY`。031 提供会计期间 OPEN 归属、制造业基础科目模板、辅助核算、正式凭证草稿、审批记账、总账、明细账、科目余额和试算平衡基础；不提供 032 财务结账与资金税务基础、期间关闭/反关闭、银行对账、税务基础、期末损益结转或正式财务报表。
- 凭证与来源：028 READY 六类来源由后端重新读取原业务事实并按 7 条固定 ACTIVE 过账规则生成正式凭证草稿，不复制 028 两行建议、不回写 028/029/030，不补造历史总账。手工凭证支持 `GENERAL` 和 `OPENING`，来源型草稿保留来源版本和指纹。
- 审批与账簿：正式凭证复用 022 固定审批，提交人不得自审；最终审批通过与连续编号、POSTED、不可变账簿、期间汇总、来源占用和审计同事务完成。POSTED 凭证、分录、辅助快照和账簿不可改删插，错误通过新冲销/调整凭证表达。
- 权限与验证：金额、来源、规则、凭证动作和账簿查询均以后端权限为准，来源遮蔽不得从列表、详情、候选、审批、审计或错误文本反推。V33 checksum 为 `612501943`，V29—V32 checksum 保持 `774334682`、`2130342893`、`-2074547591`、`249406902`；正式完整验证器 142/142、failed 0、数据库/MinIO 对象 18/18，隔离验证历史证据 140/140，GL 门禁 `activeRules=7`、`activeLines=17`、`activeAuxMaps=9` 且违反数为 0。
- 下一阶段：032 财务结账与资金税务基础。030 仍是业务月结与经营快照，不能等同会计期间关闭；031 总账基础也不能提前表达为财务关账。

## 2026-07-20：030 业务月结与期间快照正式交付完成

- 交付结论：030 前向追加 V32，建立独立 `periodclose` 领域，以同步检查、期间级互斥、原子关闭/重开和不可变多版本快照冻结业务月末；不回写 014、023、027、028、029 或其他上游业务事实。
- 状态与原子性：`PENDING_CHECK/BLOCKED/READY/CLOSED/REOPENED` 由后端真相驱动；关闭在同一事务内复检、生成库存/在制/项目成本/报表快照并锁定 016 期间，重开保留旧快照，再次关闭生成新版本。
- 快照与权限：固定保存库存、在制、项目成本和八类经营报表基线；金额和来源继续叠加原领域权限，`snapshot-view` 不授予总金额权限。历史手工锁期不自动补认为 030 关闭版本。
- 正式数据：正式库由 V31 自然前迁 V32；6 月历史手工锁定仍无快照，7 月执行一次合法检查后为 `BLOCKED`（3 阻断、2 警告）、仍 `OPEN`、无快照，未伪造关闭或修改 029 阻断。
- 验证结论：最终全量确认后端 652/652、前端 1017/1017、类型检查、生产构建和 V32/Stage030 通过；正式独立验证 127/127、对象 18/18、真实桌面页面和控制台通过，阻断/严重为 0。
- 承接阶段：031 会计科目、凭证与总账基础已正式交付。030 是业务月结和经营快照，不提供正式会计期间关闭、记账、总账、试算平衡或财务关账。

## 2026-07-19：029 项目成本核算正式交付完成

- 交付结论：029 已前向追加 V31，建立以销售项目为一级成本对象的独立项目成本子账，保存核算运行、来源快照、成本分录、阶段分布、调整分配、差异和经营口径毛利；不改写 009、023、027、028 或销售履约事实。
- 来源与口径：材料消费 023 库存价值事实，人工和制造费用消费 027 工单业务成本，外协消费 027 执行及 028 外协结算来源，项目费用消费 028 确认费用；成本分类与在制、完工、交付、调整阶段均由后端十进制计算并可追溯。
- 收入与毛利：目标、开票、发货三种收入口径独立展示，发货为当前主口径；隔离验收数据精确得到项目成本 838、发货口径累计经营毛利 9162。上述结果是经营管理口径，不是正式收入确认、会计凭证或总账。
- 当前性与调整：核算运行以完整来源指纹判断 `CURRENT`/`STALE`；取消运行不取代有效当前结果，确认前会重新校验来源。成本调整和公共制造费用分配复用 022 固定审批，确认后只形成 029 独立分录。
- 验证结论：全量窗口完成后端 623 项、前端 995/995、类型检查、生产构建、V1/V30 到 V31、独立验证 117/117、真实 API 精确数据和桌面页面核对；窗口缺陷经一次集中整改与差异复验闭合。正式库已自然前迁 V31，文件资源 18/18，阻断与严重问题为 0。
- 下一阶段：030 业务月结与期间快照尚未启动；029 不提前实现会计期间、月末冻结、正式凭证、总账、财务关账或正式收入确认。

## 2026-07-19：028 发票、费用与往来深化正式交付完成

- 交付结论：028 已前向追加 V30，正式交付销售/采购发票、标准采购零容差三单匹配、外协采购侧结算、项目/公共费用、预收预付、多目标核销、应收应付链接和非正式凭证草稿；V10/V11 历史往来继续作为余额权威，不补造历史发票、不改写历史金额或来源。
- 业务一致性：发票确认优先绑定既有应收/应付，缺失时按合法来源唯一生成；收付款、预收预付和核销严格区分现金发生与余额分配，核销不重复增加现金；费用只形成供应商侧应付及项目/公共归属，外协发票不回写库存数量价值。
- 匹配决策：标准采购订单、收货和发票采用零容差匹配，任何数量、单价或金额差异都会阻止确认；外协收货使用不适用三单匹配语义，但仍保留来源、供应商、金额、应付和审计追溯。
- 凭证边界：凭证草稿是面向 031 的业务分类建议，只保存借贷方向、金额、来源、往来方和项目/公共归属，不产生正式科目、会计期间、正式凭证号、记账或总账事实。
- 验证结论：唯一全量窗口完成前端 971/971、类型检查、生产构建、V1/V29 到 V30、演示数据与独立验证 115/115；窗口后只复验缺陷差异，最终阻断和严重问题为 0。一般文案与轻微页面一致性问题登记后续治理，不改变 028 交付边界。
- 下一阶段：029 项目成本核算应消费 023 库存价值、027 生产/外协和 028 费用/外协结算归属事实，形成材料、人工、外协、制造费用、在制、完工和项目毛利；不得把 028 业务发票或凭证草稿解释为正式成本、收入确认或总账。

## 2026-07-18：028 发票、费用与往来深化启动决策

- 阶段定位：028 是发票、费用与业务结算深化层，不是正式财务总账阶段；前向新增销售/采购发票、零容差三单匹配、项目/公共费用、预收预付、多目标核销和凭证草稿。
- 历史兼容：V10/V11 应收、应付、收付款和往来冲减继续作为余额权威，不补造发票、不重写金额或来源。发票确认优先绑定已有台账，缺失时按原收发货来源生成唯一确认态台账。
- 资金决策：允许一笔真实收款/付款分配多个目标并保留未核销预收/预付余额；核销必须同主体、人民币和同项目/公共归属，不再次增加现金发生额。
- 匹配与费用：标准采购订单、入库、发票零容差匹配，差异只登记不允许确认。外协供应商结算进入采购侧但不回写库存暂估；非采购项目支出使用费用单，028 只提供 029 可消费的归属事实。
- 凭证边界：凭证草稿只包含业务分类建议、借贷方向、金额、来源、往来方和项目，不引入正式科目、会计期间、正式凭证号、记账或总账。
- 排除项：029 正式项目成本、030 月结、031 正式凭证与总账、税务申报、多币种、银行对账、员工报销和已确认发票红冲均不进入 028。

## 2026-07-18：027A 现存问题与页面规范治理完成

- 交付结论：027A 已在不改变 020、026、027 业务状态机、接口主语义、库存价值和 V29 的前提下，闭合 027 全部新增、修改和受影响页面的 `docs/ui/page-standards.md` 映射、整改与真实桌面复验，并完成已登记的服务边界、前端类型、中文占位和选择性注册等问题。
- 质量决策：功能路径可访问、控制台无错误和没有明显溢出不能替代页面规范逐项验收；后续阶段必须在唯一阶段说明中冻结受影响页面与适用门禁，由 UI 设计师审查真实页面、测试角色独立核验。
- 交付验证：显式 JDK 21 后端 573/573、前端 939/939、类型检查、生产构建、V29 空库与存量迁移、独立正式验证 114/114、正式资源 18/18 和 1280×720 桌面页面/权限矩阵均通过，阻断与严重问题为 0。
- 下一阶段：027A 独立闭合后才启动 028 发票、费用与往来深化；028 不继承 027 的历史不合规样式，也不得静默纳入 029 正式项目成本、030 月结或 031 总账。

## 2026-07-17：027 项目生产执行与外协基础完成

- 交付结论：027 已把人工确认且来源仍有效的 026 生产/外协建议幂等承接为一个项目生产工单或独立外协订单，并保留建议、运行、销售需求、项目、BOM 和目标单据双向来源；公共历史工单继续保持项目为空。
- 所有权决策：项目工单执行只能消费同项目库存或按规则受控使用公共库存，不能消费其他项目库存；领料、退料、补料和外协发料/收货复用 023 统一数量价值过账，退料回到原所有权、项目、成本层和追踪来源，不按当前均价重估。
- 生产与外协决策：工单仍保持工单级 BOM、领退补料、简单报工、完工和批次/序列追溯；外协使用独立订单、发料和收货对象，不伪装采购订单或内部工单。027 不交付工序级 MES、有限产能、APS、外协对账应付或供应商发票。
- 成本边界：项目完工和外协收货形成项目库存业务事实，必要时保留暂估估值状态；027 不计算正式在制、完工、制造费用、外协结算、项目毛利或财务凭证，这些由 028/029 承接。
- 架构决策：对既有大型生产、反向业务和规划服务采用触达式增量拆分，新增建议转换、所有权策略、库存过账协调、幂等、生产反向业务和外协服务；前端拆分计划/生产菜单、路由、工单详情部件及外协页面族，不进行高风险全量重写。
- 交付验证：阻断/严重问题为 0；V29 空库及 V28 存量升级、受影响后端 92/92、前端 125/125、备注专项 17/17、类型检查、生产构建和 1280×720 真实桌面浏览器链路通过，正式资源保持 18/18。
- 下一阶段：028 发票、费用与往来深化应消费 024/025/027 已形成的采购、销售、项目生产和外协事实，不回写历史单据，也不得提前替代 029 正式项目成本。

## 2026-07-17：026 订单驱动缺料与供给建议完成

- 交付结论：026 已以 025 有效销售需求为唯一正式输入，交付项目/公共边界明确的 BOM 展开、基本单位净算、确定性供给分配、混合快照、来源指纹、过期重算、四类建议和来源追溯。
- 供给决策：项目需求不得自动使用其他项目资源，公共需求不得使用项目专属资源；同一库存、采购或工单供给不能重复分配。替代料只提示，公共库存使用只建议，不自动改变库存、预留、所有权或业务单据。
- 建议决策：采购请购建议、生产建议、公共库存使用建议和既有供给使用建议是固定类型。只有人工确认的采购建议可在 026 转换权限与 024 请购创建权限同时满足时幂等生成 `DRAFT` 请购；不自动提交审批、不选价、不生成采购订单。生产和外协建议等待 027 承接。
- 快照决策：运行时读取当前 021/023/024/025 事实并持久化需求、分配、异常、建议、来源和 BOM 快照；查询只读历史快照，重算产生新运行，不用当前 BOM 反向改写旧结果。
- 交付验证：后端 550/550、前端 879/879、类型检查、生产构建、V28 空库/存量迁移、正式资源 17/17、独立验证 114/114、主分支 1280×720 浏览器与空态视觉均通过；正式库已自然前迁到 V28。
- 下一阶段：027 项目生产执行与外协基础需要承接项目工单归属、领退补料、简单报工、完工和外协执行，但不扩展为工序级 MES、有限产能或 APS。

## 2026-07-15：023 完成交付与全量演示数据治理

- 交付结论：023 已完成公共库存移动加权平均、项目实际来源成本层、不可变价值流水、仓库调拨、所有权转换、盘点盘差、期初估值、暂估调整、成本权限脱敏和固定审批闭环，已进入 `main` 并升级到 V24。
- 当前边界：项目已经进入销售项目/合同、库存所有权和项目库存成本层；尚未进入 024 项目专采、027 项目生产归集、028 发票费用、029 正式项目成本或正式财务。
- 演示数据决策：全量电气制造演示数据作为独立运维数据基线，通过真实 API/业务服务生成；不得写入 Flyway，不得纯 SQL 直灌库存价值、审批终态、文档任务终态或附件元数据。
- 迁移边界：023 正式交付版本仍保留为 V24 历史事实；全量真实 API 重建发现生产补料价值流水类型字段过短，因此追加 V25 仅扩展 `inv_value_movement.movement_type` 长度。V25 是 023 缺陷前向修复，不包含演示事实，不代表 024 已启动。
- 正式数据状态：2026-07-15 已通过受控 R3 入口重建 `qherp/qherp-private`，Flyway V25、独立验证 114/114，MinIO 17 个对象与数据库 17 条可用文件对象一致；演示数据覆盖 000 至 023，不虚构 024 及以后事实。
- 测试隔离决策：Maven 测试对象存储默认失败关闭，需要真实对象存储的集成测试必须显式使用独立 MinIO Testcontainer。全量测试前后共享 bucket 必须与数据库文件对象数量一致；发现测试污染时先修复隔离，再受控重建，不能仅手工清理孤儿对象。
- 协作决策：后续阶段复用五个 `gpt-5.5`、`xhigh` Codex 后台固定角色会话，按“目标讨论—唯一阶段说明—整包开发—集中审查—集中整改—差异复审—唯一全量验证—合入交接”推进。
- 下一阶段：024 采购深化与项目专采尚未启动；启动前必须先冻结请购、询价比价、价格协议、项目专采/公共采购和 023 成本层接入边界。

## 2026-07-12：第一版完整 ERP 路线重定向

- 决策：在 000 至 019 基础能力完成后，第一版后续路线从“精细制造执行优先”调整为“项目型制造的业财成本闭环优先”。
- 业务背景：主要产品是配电柜等项目型、大件制造产品，第一版暂不需要工艺路线、工作中心、精细工序流转、复杂质量检测或产能排程；用户更重视项目实际成本、库存计价、发票、月结和正式财务。
- 一级对象：销售项目是一级成本、收入和利润对象。一个项目允许包含一个主合同、多个补充合同和多次追加订单。
- 归集关系：合同负责收入、开票、回款和变更归属；销售订单、采购、生产工单、费用、发票、收付款和成本归集到项目；工单作为成本明细对象，柜体序列号作为追溯和必要下钻对象。
- 材料来源：项目专门采购与公共库存领用并存。项目专采库存保留项目归属，公共库存通过领料进入项目。
- 计价规则：项目专采材料按项目和实际采购批次成本计价；公共库存按移动加权平均计价。项目专属库存转公共库存或跨项目转移必须保留原因、审批和价值承接记录。
- 生产深度：第一版保留工单级 BOM、领退补料、简单报工、完工入库、外协和序列追溯；完整 MPS、APS、工艺路线、工作中心、工序派工、精细在制、完整 QMS 和 SPC 后置。
- 阶段调整：旧 020 至 036 编号含义在实际启动前废止，重新编排为项目合同、成本财务主数据、固定审批与单据平台、库存计价、采购深化、销售深化、轻量缺料、项目生产、发票费用、项目成本、月结、总账、财务结账、经营财务分析、交付治理和最终验证。
- 权威路线：详细阶段目标、依赖和边界见 `docs/handoffs/2026-07-12-project-handoff-current.md`。
- 当前状态：000 至 023 已完成实现、全量验证和桌面浏览器验收；024 至 036 的规划内容不得表达为当前已交付能力。

## 2026-07-14：022 固定审批与业务单据平台基础完成

- 交付结论：合同生效与 BOM 工程变更应用作为首批固定审批样板，已经形成审批实例、任务、待办/已办/我发起、消息、权限、并发版本、幂等、自批限制和审计闭环；业务状态与审批状态保持分离。
- 文件与打印：合同/ECO 附件使用私有 MinIO 对象存储并继承业务权限；审批提交固化附件快照并在审批中锁定变更。两个固定打印模板支持预览、模板代码/版本识别、PDF 任务和重新鉴权下载，不建设模板设计器。
- 导入导出：物料只支持新增导入与当前筛选导出，BOM 只支持单个草稿创建/更新导入与草稿导出；两者都保留手工录入，经过暂存、校验、确认、业务服务和原子提交，不扩展为全系统手工页面批量导入。
- 历史兼容：V19 空库迁移和 V18 存量升级通过，000 至 021 历史业务、BOM 时效、ECO、合同、工单快照和权限保持兼容。
- 范围边界：不建设通用工作流、动态表单、外部通知、电子签章、通用对象存储平台、全系统导入、库存计价、项目专采、发票、项目成本、月结或总账。
- 后续承接：023 已按固定五角色冻结的项目/公共库存、成本层、移动加权平均、调拨、盘点和盘差口径完成；下一阶段为 024 采购深化与项目专采。

## 2026-07-12：020 销售项目与合同管理基础完成

- 交付结论：销售项目、主合同、补充合同和销售订单可选项目合同关联已形成可权限控制、可审计、可供后续阶段引用的稳定 `projectId` 主维度。
- 历史兼容：000 至 019 的历史销售订单允许项目和合同为空，不按客户、日期或金额伪造归属；既有确认、出库、往来、报表和追溯路径继续可用。
- 权限澄清：项目负责人候选允许项目查看、创建或更新任一权限调用，只返回启用用户最小字段，不要求系统用户列表查看权限。
- 验证结果：后端 309 项、前端 596 项测试通过，类型检查、生产构建、空库和存量库迁移、浏览器主路径、权限路径及桌面和 390px 视觉审计通过。
- 下一阶段：021 成本财务主数据与 BOM 治理；不得在 021 提前实现库存计价、固定审批、项目专采、正式成本或总账。

## 2026-07-10：业务期间与历史数据保护基础

- 决策：启动 016 业务期间与历史数据保护基础阶段，以业务日期为边界维护开放和锁定期间。
- 原因：015 已要求通过反向业务而非直接改写已过账原单表达修正，但历史业务日期的新写入仍可能污染库存、采购、销售、生产、成本、往来和经营报表口径。
- 范围：提供期间维护、锁定、解锁、独立权限、审计和服务层统一写入保护；锁定期间保持查询、追溯和报表查看。
- 跨期规则：开放期间可引用锁定期间历史原单创建反向业务，但反向影响按自身业务日期进入开放期间，不回写历史数据。
- 排除：本阶段不交付正式财务关账、会计期间、总账、凭证、税务、期间快照或成本结转。

## 2026-07-10：质量检验与库存质量状态基础

- 决策：启动 017 质量检验与库存质量状态基础阶段，将库存余额从仓库+物料升级为仓库+物料+质量状态，并把合格库存作为唯一可用库存口径。
- 原因：现有 `availableQuantity` 等于现存量，无法区分待检、不合格和冻结库存，可能导致销售出库、生产领料、生产补料和后续计划净算误用不可用库存。
- 范围：建立待检、合格、不合格和冻结四类库存质量状态；历史库存默认迁移为合格；采购入库、完工入库、销售退货和生产退料默认形成待检库存；轻量质量确认支持待检转合格、不合格或冻结；合格库存可冻结并解冻回合格。
- 可用量规则：`availableQuantity` 只代表合格可用库存。销售出库、生产领料和生产补料只能消耗合格库存；总库存充足但合格库存不足时必须明确提示质量状态不可用或指定质量状态库存不足。
- 退货规则：采购退货必须显式选择质量状态，允许待检、不合格或合格库存退回供应商；冻结库存需先解冻。
- 期间保护：质量确认、冻结、解冻、入库质量状态落账和按质量状态扣减库存均受 016 业务期间保护、权限、原因和审计约束。
- 排除：本阶段不交付完整 QMS、检验标准、抽样方案、SPC、批次、序列号、库位、MRP、审批流、正式财务、复杂返工返修或质量成本归集。

## 目标

记录已经确认的产品方向和阶段性决策，避免后续模块开发反复讨论同一问题。

## 已确认决策

### 项目定位

本项目是制造业单公司内部使用的 Web ERP 系统，不做零售、门店收银、电商或多租户平台方向。长期覆盖基础资料、采购、销售、库存、生产、成本、财务和报表；第一版后续建设以销售项目为主线，库存计价、项目专采、销售履约、项目生产/外协、业务发票费用往来、项目成本子账、业务月结、总账和财务关闭已由 023 至 032 正式补齐，033 固定经营财务分析已在交付分支完成并待最终合入交付，生产保持工单级可信闭环。

### 首个业务模块

首个业务模块为账号与权限基础。

原因：

- 所有后续模块都依赖登录态、用户身份、角色权限、菜单入口和接口鉴权。
- 先完成账号权限可以让后续模块从第一天起按真实权限场景验收。
- 避免物料、BOM、库存和生产模块完成后再反向补权限，造成返工。

### 第二个业务模块

第二个业务模块确认为基础资料与物料管理，采用轻量基础资料 + 物料管理范围。

纳入计量单位、仓库、供应商、客户、物料分类和物料档案。该阶段只做主数据维护，不进入 BOM、库存流水、采购销售单据或财务核算。

原因：

- 物料、BOM、库存和生产工单都依赖稳定主数据。
- 仓库、供应商、客户是常规 ERP 后续采购、销售、库存模块的前置资料。
- 提前完成轻量主数据可以避免后续模块反复补字段和补权限。

### 第三个业务模块

第三个业务模块确认为 BOM 管理，采用标准 BOM 主数据 + 版本管理 + 明细维护范围。

纳入 BOM 列表、详情、新增、编辑草稿、复制版本、启用、停用、明细用料维护、启用版本唯一、物料引用校验、可检测循环引用校验、权限控制、审计记录和浏览器视觉分析。该阶段不进入库存、生产工单、领料、报工、完工入库、成本、采购销售、工艺路线、替代料、选配 BOM、工程变更审批或复杂多级展开 UI。

原因：

- BOM 是物料档案之后、生产工单之前的关键主数据。
- 后续领料、工单用料和成本归集都需要稳定的父子物料结构和版本口径。
- 先完成 BOM 版本和启用规则，可以避免生产模块直接依赖临时用料结构。
- 保存和启用时拒绝可检测循环引用，能够降低后续工单引用错误 BOM 的风险。

### 第四个业务模块

第四个业务模块确认为仓库与库存基础能力，采用库存余额 + 库存变动流水 + 期初/调整单据范围。

纳入库存余额查询、库存变动流水查询、期初库存草稿与过账、库存调整草稿与过账、库存数量不可为负、同一仓库物料期初只允许过账一次、库存变动追溯、权限控制、审计记录和浏览器视觉分析。该阶段不进入生产工单、领料、报工、完工入库、成本核算、采购销售单据、批次、序列号、库位、条码、调拨、盘点任务、库存占用锁定、缺料分析、先进先出、多单位换算、多组织或多租户。

原因：

- 库存是 BOM 之后、生产工单和领料之前的关键基础能力。
- 后续领料、完工入库和成本归集都依赖可信库存余额和可追溯变动记录。
- 先建立期初和调整两类受控库存来源，可以避免后续生产模块直接修改库存余额。
- 一期不允许负库存、已过账单据不直接回改，有利于保证数据可信度和审计追溯。

已确认口径：

- 库存基础阶段只按“仓库 + 物料”维度管理余额，不引入批次、库位、序列号或条码。
- 一期可用数量等于现存数量，锁定数量固定为 0 或仅作为响应预留，不启用库存占用。
- 期初和调整均先保存草稿，再通过过账写入余额和流水。
- 已过账单据不可编辑；如需修正，通过新的库存调整单处理。
- 数量入账使用物料基本单位，不做单位换算。

### 第五个业务模块

第五个业务模块确认为生产执行基础能力，采用生产工单 + 领料 + 报工 + 完工入库范围。

纳入生产工单列表、详情、新增、编辑草稿、发布、完成、取消；工单发布时生成 BOM 用料快照；基于工单创建、编辑和过账生产领料；基于工单创建、编辑和过账生产报工；基于工单创建、编辑和过账完工入库；领料扣减库存、完工入库增加库存；生产来源库存流水追溯；权限控制、审计记录和浏览器视觉分析。该阶段不进入高级排产、工艺路线、工序流转、批次、序列号、库位、条码、倒冲、退料、补料、返工、委外、采购销售联动、审批流、正式成本核算、多单位换算、多组织或多租户。

原因：

- 生产执行是库存之后、成本归集之前的一期核心链路。
- 工单需要稳定引用已启用 BOM，并把用料结构冻结为快照，避免后续 BOM 修改影响历史生产任务。
- 领料和完工入库必须复用库存过账与流水追溯能力，不能直接旁路修改库存余额。
- 报工为完工入库和后续成本归集提供生产结果口径。
- 一期先打通可信生产闭环，比直接进入排产、工艺路线或复杂成本更符合当前基础阶段。

已确认口径：

- 工单编号由后端自动生成。
- 工单发布时生成 BOM 用料快照，已发布工单不再受 BOM 后续修改影响。
- 一期不启用库存占用锁定，领料过账时实时扣减库存。
- 一期不允许超领、超报、超入。
- 报工采用草稿和过账模式，报工过账只更新生产结果，不直接修改库存。
- 完工入库累计数量不得超过累计合格报工数量。
- 领料、报工和完工入库先作为工单上下文能力，不建立独立生产单据中心。
- 窄屏侧栏占用空间不单独作为阻断；若导致核心操作不可见、表单不可提交或权限状态无法识别，则作为阻断视觉缺陷。

### 第六个业务模块

第六个业务模块确认为成本归集基础记录，采用成本业务记录 + 来源追溯 + 手工补充记录范围。

纳入成本记录列表、详情、手工记录、生产领料自动材料来源记录、生产报工自动人工数量口径记录、完工入库产出数量追溯、生产工单详情成本归集分区、权限控制、审计记录和浏览器视觉分析。该阶段不进入正式财务核算、总账、凭证、会计期间、结账、标准成本、移动加权、先进先出、成本差异分析、在产品成本、完工成本分摊、制造费用自动分配、采购销售联动、多币种、多组织、多公司、多账套、多租户、审批流或 BI 报表。

原因：

- 成本归集是生产执行之后、财务和报表能力之前的一期桥梁。
- 领料、报工、完工入库已经形成可信生产来源，下一步需要沉淀为可追溯的成本业务记录。
- 009 成本归集阶段当时库存和生产执行尚无稳定单价来源，因此该阶段不直接承诺正式材料成本金额；023 已补齐库存计价，但没有把 009 业务成本记录升级为正式项目成本或财务核算。
- 先建立业务记录、来源单据、数量和金额口径，可以为后续正式核算规则提供数据基础。
- 把成本记录独立建模，避免把后续财务能力混入生产执行服务。

已确认口径：

- 一期成本归集只做业务记录和追溯，不承诺正式财务核算结果。
- 自动材料来源记录来自已过账生产领料明细，只记录数量、单位、物料、工单、产品和来源单据，不自动计算材料金额。
- 自动人工来源记录来自已过账生产报工，只记录报工数量口径，不自动计算人工金额。
- 完工入库作为产出数量和归集对象追溯，不作为成本类型计入金额。
- 手工记录可用于人工金额、制造费用、其他成本或必要材料金额补录。
- 手工记录必须关联有效生产工单，产品从工单带出；本阶段不支持仅按产品录入成本记录。
- 手工记录必须填写成本类型、业务日期、金额或数量口径和说明。
- 成本记录不得反写库存余额、生产工单状态或生产单据状态。
- 页面文案必须明确“业务记录与追溯”，避免被理解为正式财务成本核算结果。

### 一期验收方式

一期采用模块逐步验收，并在生产主链路完成后追加端到端闭环验收。

模块逐步验收用于控制交付节奏；端到端闭环验收用于验证物料、BOM、库存、工单、领料、报工、完工入库和成本记录之间的数据贯通。

### 一期生产主链路端到端闭环验收与稳定化

一期六个业务里程碑完成后，追加一期生产主链路端到端闭环验收与稳定化阶段。

该阶段确认从账号权限、基础资料与物料、BOM、库存、生产执行到成本归集的完整链路真实可用，并集中验证权限一致、数据可追溯、页面可操作、异常受控和阻断缺陷已修复。

该阶段不是第七个业务模块，不新增采购、销售、正式财务、报表、审批、多组织、多租户、工艺路线、批次、库位、条码或正式成本核算能力。发现相关诉求时只登记为后续候选，不纳入本阶段验收范围。

### 一期后第一个业务模块

一期后第一个业务模块确认为采购管理基础模块，采用采购订单 + 采购入库 + 库存增加 + 来源追溯范围。

纳入采购订单列表、详情、新建草稿、编辑草稿、确认、取消、关闭、明细维护；基于已确认或部分入库采购订单创建采购入库草稿、编辑草稿、过账；采购入库过账后增加库存余额、生成 `PURCHASE_RECEIPT` 库存流水；采购订单、采购入库和库存流水三方追溯；权限控制、审计记录和浏览器视觉分析。该阶段不进入应付、发票、付款、税务、审批、询价、比价、供应商评分、MRP、采购退货、质检入库、暂估入库、多币种、多组织、正式财务凭证或报表分析。

原因：

- 一期已经完成从库存到生产执行再到成本归集的内部闭环，但原材料库存来源仍主要依赖期初和库存调整。
- 采购管理补齐制造业原材料进入企业的真实业务入口。
- 采购入库可以复用库存余额和库存流水追溯机制，技术边界清晰。
- 采购基础稳定后，后续应付、发票、付款、销售和正式财务能力才有可靠业务来源。

已确认口径：

- 采购作为独立业务域，不并入库存单据或生产域。
- 采购订单状态包含草稿、已确认、部分入库、已入库、已关闭和已取消。
- 采购入库状态包含草稿和已过账。
- 采购入库过账必须生成 `PURCHASE_RECEIPT` 库存流水，并通过来源字段追溯到采购入库明细。
- 采购单价只作为采购业务记录字段，不生成应付、不计算税额、不形成正式成本核算结果。
- 本阶段统一使用物料基本单位，不做采购包装单位或单位换算。
- 已过账采购入库不可编辑，不支持反过账；必要修正通过库存调整并保留说明。

### 一期后第二个业务模块

一期后第二个业务模块确认为销售管理基础模块，采用销售订单 + 销售出库 + 库存扣减 + 来源追溯范围。

纳入销售订单列表、详情、新建草稿、编辑草稿、确认、取消、关闭、明细维护；基于已确认或部分出库销售订单创建销售出库草稿、编辑草稿、过账；销售出库过账后扣减库存余额、生成销售来源库存流水；销售订单、销售出库和库存流水三方追溯；权限控制、审计记录和浏览器视觉分析。该阶段不进入应收、发票、收款、税务、正式收入确认、销售报价、合同、信用额度、价格体系、折扣政策、审批流、销售退货、换货、发运物流、对账单、MRP、销售预测、产销排程、报表看板、多币种、多组织、多公司、多账套、正式财务凭证或多租户。

原因：

- 采购管理基础已经补齐原材料进入企业的采购入库链路，下一步需要补齐成品或半成品离开企业的销售出库链路。
- 销售出库可以复用库存余额和库存流水追溯机制，业务边界清晰。
- 销售订单与销售出库完成后，系统具备采购入库、生产执行、库存扣减和销售来源追溯的更完整制造业业务闭环。
- 销售基础稳定后，后续应收、发票、收款、经营看板和正式财务能力才有可靠业务来源。

已确认口径：

- 销售作为独立业务域，不并入库存单据或生产域。
- 销售订单状态包含草稿、已确认、部分出库、全部出库、已关闭和已取消。
- 销售出库状态包含草稿和已过账。
- 销售出库过账必须生成销售来源库存流水，并通过来源字段追溯到销售出库明细。
- 销售出库相关契约命名统一使用 `SALES_SHIPMENT`、`sales:shipment:*` 和 `/sales/shipments`，不使用 outbound 命名。
- 本阶段销售订单明细只允许选择启用的 `FINISHED_GOOD` 与 `SEMI_FINISHED` 物料。
- 启用的 `RAW_MATERIAL` 和 `AUXILIARY` 在本阶段视为不可销售物料，创建、更新或确认销售订单时必须拒绝。
- 本阶段不新增物料“可销售”字段，不扩大物料主数据范围。
- 销售订单创建、更新、确认时客户必须存在且启用。
- 销售订单确认后客户若被停用，不阻止基于该已确认或部分出库订单创建、更新或过账销售出库；销售出库仍必须校验订单状态、来源行、物料、单位、仓库和库存。
- 销售单价只作为销售业务记录字段，不生成应收、不计算税额、不确认收入、不形成正式财务凭证。
- 本阶段统一使用物料基本单位，不做销售包装单位或单位换算。
- 已过账销售出库不可编辑，不支持反过账；必要修正通过库存调整并保留说明。
- 销售出库库存不足必须拒绝，不允许负库存。

### 一期后第三个业务模块

一期后第三个业务模块确认为财务往来基础模块，采用应收台账 + 收款记录 + 应付台账 + 付款记录 + 来源追溯范围。

纳入基于已过账销售出库生成应收草稿、应收确认、取消、关闭、收款草稿、收款过账、应收余额更新、销售来源追溯；基于已过账采购入库生成应付草稿、应付确认、取消、关闭、付款草稿、付款过账、应付余额更新、采购来源追溯；权限控制、审计记录和浏览器视觉分析。该阶段不进入总账、凭证、会计科目、会计期间、结账、税务申报、完整发票生命周期、银行对账、报销、固定资产、工资、多币种汇兑损益、多组织、多公司、多账套、多租户、正式收入确认、正式成本结转、销售退货、采购退货、退款、退票或 BI 报表。

原因：

- 采购管理和销售管理基础已经形成可信采购入库、销售出库和库存追溯来源，下一步需要把采购、销售业务结果沉淀为往来台账。
- 应收和应付是正式财务核算之前的关键业务桥梁，先做业务台账与收付款登记，可以避免直接引入凭证、科目和结账造成范围过大。
- 收付款登记必须保证余额、状态和来源一致，为后续正式财务、发票、对账和报表能力提供可信业务数据基础。
- 将财务往来独立建模，可以避免把收付款逻辑混入采购、销售或库存服务。

已确认口径：

- 财务往来基础只做业务台账、收付款登记、余额核销和来源追溯，不承诺正式财务核算结果。
- 应收来源固定为已过账销售出库，金额按销售出库明细数量和来源销售订单单价形成业务金额快照。
- 应付来源固定为已过账采购入库，金额按采购入库明细数量和来源采购订单单价形成业务金额快照。
- 同一销售出库明细只能生成一次应收来源；同一采购入库明细只能生成一次应付来源。
- 应收状态包含草稿、待收款、部分收款、已收清、已关闭和已取消。
- 收款状态包含草稿、已过账和已取消；已过账收款不可编辑、不可取消。
- 应付状态包含草稿、待付款、部分付款、已付清、已关闭和已取消。
- 付款状态包含草稿、已过账和已取消；已过账付款不可编辑、不可取消。
- 收付款过账必须在事务内更新核销明细、累计金额、未结余额和状态；失败不得产生部分写入。
- 收款金额不得超过未收金额，付款金额不得超过未付金额。
- 页面文案必须明确“财务往来、应收台账、收款记录、应付台账、付款记录、来源追溯”，不得表达为总账、凭证、税务、完整发票或 BI 报表能力。

### 成本归集一期深度

一期成本归集做到业务记录，不承诺正式财务核算结果。

业务记录应至少能说明：

- 成本来源。
- 关联生产工单和产品。
- 成本类型。
- 成本金额或数量口径。
- 记录人和记录时间。
- 后续可追溯的来源单据。

正式核算结果需要稳定的物料、库存、工单、领料、报工、入库和财务规则支撑，后续阶段再展开。

### 一期后第四个业务模块

一期后第四个业务模块确认为经营报表基础模块，采用固定口径经营报表 + 来源追溯 + 经营异常清单范围。

纳入经营概览、销售经营汇总、采购经营汇总、库存收发存汇总、生产执行汇总、成本归集汇总、应收应付与收付款汇总、经营异常清单、报表权限控制、来源追溯、浏览器验收和视觉分析。该阶段不进入可配置 BI、拖拽报表、自定义 SQL、总账、凭证、会计科目、会计期间、结账、正式财务报表、税务申报、银行对账、多组织、多公司、多账套、多租户、销售退货、采购退货、退款、反冲、期间关账或经营大屏。

原因：

- 当前采购、销售、库存、生产、成本和财务往来基础模块已经形成可信业务数据，需要固定经营视图帮助管理者核对业务状态。
- 报表基础可以反向验证跨模块数据一致性，暴露来源追溯、权限和业务状态口径问题。
- 采用固定口径报表可以避免过早建设复杂 BI 平台，也避免把经营金额误交付为正式财务报表。
- 报表基础稳定后，后续退货退款、业务反冲和期间关账才有明确的数据影响范围。

已确认口径：

- 报表一期只做固定口径经营报表，不做可配置报表或拖拽式 BI。
- 报表只读，不生成、修改或删除任何业务单据。
- 销售金额以已过账销售出库和来源销售订单单价形成经营金额，不等同正式收入确认。
- 采购金额以已过账采购入库和来源采购订单单价形成经营金额，不等同正式成本入账。
- 库存收发存第一版按库存流水业务日期和流水顺序推算，不建立期间快照。
- 成本归集报表只展示业务成本记录，必须明确不属于正式财务核算。
- 应收应付与收付款报表只展示业务往来台账和余额，不属于总账余额表。
- 所有报表结果必须能追溯来源；来源详情跳转继续受来源模块权限控制。

### 一期后第五个业务模块

一期后第五个业务模块确认为退货退款与业务反冲基础模块，采用销售退货 + 采购退货 + 生产退料/补料 + 往来冲减 + 经营报表净额同步范围。

纳入基于已过账销售出库的销售退货、退货入库库存流水、应收冲减和销售净额报表；基于已过账采购入库的采购退货、退货出库库存流水、应付冲减和采购净额报表；基于已过账生产领料和工单物料的生产退料、补料、库存反向或补充流水、成本业务净额；往来冲减和退款/付款冲减业务记录；来源追溯、权限控制、审计记录、浏览器验收和视觉分析。该阶段不进入审批流、正式红字发票、税务处理、银行退款支付、总账凭证、会计期间、结账、多组织、多币种、高级质量体系、复杂售后维修、换货维修流程或正式财务核算。

原因：

- 采购、销售、库存、生产、成本、往来和经营报表正向链路已完成，真实 ERP 使用中必须支持错误修正和反向业务闭环。
- 经营报表基础已经提供跨模块经营数据视图，若没有退货、退款、反冲和净额口径，报表数据无法解释真实业务变化。
- 已过账原单不可直接修改是当前系统数据可信度的核心原则，反向业务必须以新增记录方式表达。
- 先补齐退货退款与业务反冲，比直接进入期间关账、质量体系或计划排产更能保护已有业务数据可信度。

已确认口径：

- 已过账原单是历史业务事实，退货、退款、退料、补料和反冲不得直接改写或删除原单。
- 销售退货基于已过账销售出库行，过账后生成销售退货入库库存流水，并冲减应收业务余额。
- 采购退货基于已过账采购入库行，过账后生成采购退货出库库存流水，并冲减应付业务余额。
- 生产退料基于已过账生产领料行，过账后生成退料入库流水，并影响工单材料消耗和成本业务净额。
- 生产补料基于有效生产工单和工单物料行，过账后生成补料出库流水，并影响工单材料消耗和成本业务净额。
- 往来冲减只作为业务余额调整和来源追溯，不作为正式银行退款、税务、总账或凭证处理。
- 草稿反向单不影响库存、往来、成本和报表；已过账反向单才进入经营数据。
- 经营报表必须能区分原发生、反向发生和净额，并能追溯反向来源。
- 来源权限不足时继续脱敏，不能通过反向业务或报表绕过原模块权限。

## 待后续模块确认

- 正式项目收入确认采用发货、业务开票、履约进度还是多口径并存；028 已交付业务开票事实，但未裁决正式收入确认时点。
- 项目专采余料转公共库存、跨项目转移和跨期退料的价值承接规则。
- 人工和制造费用的采集与分摊依据，以及项目在制和完工结转时点。
- 数据范围是否需要按仓库、部门、项目负责人或业务线限制。
- 正式财务采用的会计准则、科目模板、税率、票种、红字处理和税务申报深度。
- 固定资产、工资薪酬和多币种是否进入第一版；本轮尚未确认。

## 决策变更规则

任何产品决策变更必须记录：

- 变更原因。
- 影响模块。
- 影响任务文档和验收标准。
- 是否影响当前阶段成果。
- 主代理决策结论或用户确认结果。
