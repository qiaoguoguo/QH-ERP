# 系统代码逻辑知识层实施计划

1. 冻结证据白名单、结构、摘要和安全规则。
2. 新增离线生成器，从前后端源码抽取结构化逻辑证据。
3. 新增 V42 快照与证据表，保证完整快照原子切换和重复导入幂等。
4. 新增后端启动导入器、只读证据查询服务和人工知识/逻辑知识混合上下文服务。
5. 验证生成器确定性、索引安全、迁移、幂等导入和混合检索。
6. 重启本地环境并确认 V42 与激活快照。
7. 在开始 AI 问答界面设计和开发前，向用户提交实现结果并等待确认。
