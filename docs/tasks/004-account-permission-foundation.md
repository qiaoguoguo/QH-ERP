# 任务记录：账号与权限基础模块

## 任务目标

实现 QH ERP 的首个业务基础模块，使企业内部用户能够登录系统，并按角色权限访问菜单、页面、按钮和后端接口，为后续物料、BOM、库存和生产模块提供统一权限底座。

## 背景与输入

- 关联业务背景：本项目是制造业单公司内部 Web ERP，所有后续模块都依赖账号、角色、菜单、按钮和接口权限。
- 关联文档：
  - [账号与权限基础模块范围](../product/account-permission-scope.md)
  - [账号与权限需求说明](../product/account-permission-requirements.md)
  - [账号与权限接口契约](../api/account-permission-api.md)
  - [账号与权限测试计划](../testing/account-permission-test-plan.md)
  - [接口契约基线](../api/api-contract-baseline.md)
  - [本地开发与验证说明](../ops/local-development.md)
- 已确认约束：
  - 只支持企业内部用户。
  - 不做外部账号、多租户、复杂审批、单点登录和第三方登录。
  - 后端接口鉴权是最终安全边界，前端权限表现只作为用户体验控制。
  - 阶段成果必须本地部署、浏览器可访问、用户可操作。

## 范围

### 本次包含

- 登录、退出、当前用户信息。
- 初始超级管理员初始化。
- 用户列表、创建、编辑、启用、停用、重置密码。
- 角色列表、创建、编辑、启用、停用。
- 用户分配角色。
- 角色分配菜单权限和按钮权限。
- 后端接口权限校验。
- 停用账号访问拦截。
- 账号、角色、权限变更审计记录。
- 前端后台布局、登录页、用户管理、角色管理、权限配置、无权限反馈。
- 本地部署、接口、前端、后端和浏览器验收。

### 本次不包含

- 外部客户、供应商或会员账号。
- 多公司、多组织、多租户隔离。
- 复杂审批流。
- 单点登录或第三方登录。
- 完整字段级权限配置界面。
- 按仓库、部门或业务线配置复杂数据范围。
- 完整操作审计查询报表，仅实现账号权限变更的基础审计写入和必要查询。

## 角色分工

- 产品经理：维护需求范围、业务规则、验收路径和字段说明。
- UI 设计师：维护页面结构、交互状态、权限表现和浏览器设计验收。
- 前端开发：实现页面、路由守卫、菜单渲染、按钮权限、接口联调和前端测试。
- 后端开发：实现认证会话、用户角色权限模型、接口、数据库迁移、初始化数据、鉴权和后端测试。
- 测试：执行功能、接口、权限、异常、回归和部署验收。
- 主代理：统筹计划、调度子代理、集成实现、运行验证、提交阶段结果。

## 预期成果

- 文档成果：
  - 本任务文档。
  - 账号与权限需求说明。
  - 账号与权限接口契约。
  - 账号与权限测试计划。
  - 实施计划。
- 代码成果：
  - 后端用户、角色、权限、认证、鉴权、初始化和审计能力。
  - 前端登录、当前用户、后台菜单、用户管理、角色管理、权限配置页面。
  - 数据库迁移和初始化数据。
  - 前后端自动化测试。
- 验收成果：
  - 本地服务可启动。
  - 浏览器可登录并完成账号权限核心路径。
  - 越权接口被后端拒绝。
  - 停用账号无法登录或继续访问受保护资源。

## 验收标准

- 功能验收：
  - 初始超级管理员可登录。
  - 管理员可创建用户、角色并分配角色权限。
  - 不同角色登录后菜单和按钮表现不同。
  - 用户停用后不能登录，已登录访问也应被后端拒绝。
- 数据验收：
  - 用户名、角色编码、权限编码具备唯一约束。
  - 初始化数据可从空库迁移并幂等创建。
  - 账号、角色、权限变更有基础审计记录。
- 权限验收：
  - 未登录访问受保护页面和接口会被拦截。
  - 无权限用户直接请求接口返回无权限。
  - 权限变更以后端实时校验为准；前端菜单和按钮通过刷新当前用户或重新登录更新。
- UI 验收：
  - 登录、后台布局、用户列表、角色列表、权限配置、无权限页具备加载、空状态、错误和成功反馈。
  - 页面保持 ERP 后台信息密度，不做营销化布局。
- 测试验收：
  - 前端 `npm test`、`npm run typecheck`、`npm run build` 通过。
  - 后端 Docker 化 JDK 21 + Maven + Testcontainers 测试通过。
  - 权限和接口关键场景具备自动化测试或明确浏览器验收记录。
- 部署验收：
  - `docker compose up -d postgres` 可启动本地数据库。
  - 后端和前端可本地启动。
  - 用户可通过浏览器完成核心验收路径。

## 风险与处理

- 权限只做前端隐藏：必须由后端接口鉴权覆盖，测试直接调用接口验证。
- 权限变更生效口径不清：本模块明确后端实时生效，前端通过刷新当前用户或重新登录更新展示。
- 初始化管理员不可用：作为阻断缺陷处理。
- 密码和会话安全不足：采用 HttpOnly Cookie + 服务端 Session，新增、修改、删除接口保留 CSRF 或等效校验。
- 菜单树和权限树半选规则争议：需求和接口文档中明确父子层级和半选只属于前端展示状态，后端保存明确权限点集合。

## 变更控制

- 变更原因：账号权限模块是首个业务基础模块，需要在实现前明确可验收范围。
- 影响范围：前端、后端、数据库、接口、测试、部署验收。
- 涉及文件：`docs/product`、`docs/api`、`docs/testing`、`apps/web`、`apps/api`。
- 验收影响：任何范围变更都会影响浏览器验收路径和自动化测试。
- 决策依据：5 个子代理 `/goal` 输出结论和项目负责人汇总判断。

## 缺陷与阻断

- 阻断缺陷：初始化管理员不可用、登录不可用、未登录可访问受保护资源、越权接口成功、停用账号仍可访问、本地无法部署、浏览器核心路径不可执行。
- 严重缺陷：权限展示与后端结果不一致、审计缺失、错误提示不可定位、角色权限保存后无法恢复展示。
- 已知问题：完整字段级权限和复杂数据范围不在本期实现范围。
- 是否允许进入验收：无阻断缺陷，严重缺陷有明确处理结论且不影响本次范围时才允许。

## 执行记录

- 计划：先完成需求、接口、测试和实施计划，再按计划进入 TDD 实现；每个子任务均以任务文档目标和预期成果为准，不在执行中自行扩展范围。
- 后端实现：已完成账号、角色、权限、菜单、认证 Session、CSRF、接口鉴权、初始化数据、审计日志和后端自动化测试。
- 前端实现：已完成登录、退出、当前用户恢复、路由守卫、后台布局、用户管理、角色管理、角色权限配置、无权限页、按钮级权限展示和前端自动化测试。
- 本地部署：PostgreSQL 使用 `docker compose up -d postgres` 启动；后端使用 Docker 化 JDK 21/Maven 启动并映射到 `http://localhost:18080`；前端使用 Vite 启动到 `http://127.0.0.1:5173`，并通过 `/api` 代理访问后端。
- 浏览器功能验收：
  - 管理员 `admin` 可登录、退出，并访问用户管理、角色管理和角色权限配置。
  - 创建只读角色 `READ_ONLY_724325`，仅授予 `system:user:view` 及其父级权限。
  - 创建启用只读用户 `readonly_770598`，分配只读角色；列表显示角色正确。
  - 使用只读用户登录后，用户列表可查看，新增、编辑、分配角色、重置密码、停用等操作按钮不可见。
  - 只读用户直接访问 `/accounts/roles` 被前端路由守卫重定向到 `/forbidden?from=/accounts/roles`。
  - 只读用户直接请求后端角色接口返回 `403`，错误码 `AUTH_FORBIDDEN`。
  - 停用用户 `readonly_660563` 登录返回 `403`，错误码 `AUTH_ACCOUNT_DISABLED`。
  - 审计日志查询到 `ROLE_CREATE`、`ROLE_PERMISSION_UPDATE`、`USER_CREATE` 等账号权限变更记录。
- 浏览器视觉分析：
  - 视觉证据保存在 `docs/testing/account-permission-visual-audit/`。
  - 验收截图采用视口截图；全页截图在 Element Plus 固定列表格场景下会产生截图失真，不作为视觉证据。
  - 发现并修复用户列表账号列在桌面断行的问题：表格长文本列改为不自动断词并使用溢出提示。
  - 发现并修复移动端侧边栏挤压主内容的问题：小屏布局改为顶部导航区，主内容恢复完整宽度。
  - 发现并修复移动端固定操作列遮挡业务列的问题：小屏取消固定列 sticky 行为，使用横向滚动承载宽表格。
- 验证：
  - 前端 `npm test` 通过，8 个测试文件、47 个用例。
  - 前端 `npm run typecheck` 通过。
  - 前端 `npm run build` 通过。
  - 后端 Docker 化 Maven/Testcontainers `mvn -q test` 已通过。
- 结论：账号与权限基础模块达到本阶段功能、权限、接口、本地部署和浏览器视觉验收要求，可进入主分支阶段验收准备。
