# 任务记录：生产执行基础能力

## 任务名称

生产执行基础能力。

## 任务目标

建立制造业 ERP 一期生产主链路中的生产工单、领料、报工和完工入库基础能力，使已完成的物料、BOM 和库存能力能够被真实生产业务调用，并为后续成本归集提供可追溯的数据来源。

## 背景与输入

- 关联业务背景：本项目是制造业单公司内部 Web ERP，当前已完成账号权限、基础资料与物料管理、BOM 管理、仓库与库存基础能力。下一阶段应承接一期主链路中的生产工单、领料、报工和完工入库。
- 关联文档：
  - [一期范围与里程碑](../product/phase-one-scope.md)
  - [核心业务流程](../product/business-flow.md)
  - [产品决策记录](../product/product-decisions.md)
  - [仓库与库存基础任务文档](./007-inventory-management-foundation.md)
  - [BOM 管理接口契约](../api/bom-management-api.md)
  - [仓库与库存基础接口契约](../api/inventory-management-api.md)
  - [生产执行基础设计规格](../superpowers/specs/2026-07-03-production-execution-design.md)
  - [生产执行基础接口契约](../api/production-execution-api.md)
  - [生产执行基础测试计划](../testing/production-execution-test-plan.md)
- 已确认约束：
  - 本阶段以工单上下文打通生产执行闭环，不建立高级排产、工艺路线或独立生产单据中心。
  - 工单发布时生成 BOM 用料快照，后续 BOM 修改不得影响已发布工单。
  - 一期不启用库存占用锁定，领料过账时实时扣减库存，库存不得为负。
  - 一期不允许超领、超报、超入。
  - 报工只记录生产结果，不直接修改库存。
  - 领料和完工入库必须通过过账写入库存余额和库存变动流水。
  - 阶段成果必须本地部署、浏览器可访问，并保存核心路径视觉分析记录。

## 范围

### 本次包含

- 生产工单：
  - 工单列表、详情、新增、编辑草稿、发布、完成、取消。
  - 工单字段包括工单编号、产品物料、关联 BOM、计划数量、计划日期、领料仓库、入库仓库、状态、备注和审计信息。
  - 发布工单时校验产品物料、BOM、仓库和单位均有效，并生成用料快照。
- 工单用料快照：
  - 从启用 BOM 明细生成子项物料、单位、应领数量和 BOM 行来源。
  - 展示应领数量、已领数量和未领数量。
- 领料：
  - 从工单详情发起领料，生成领料草稿。
  - 明细默认来自工单用料快照，支持本次领料数量录入。
  - 领料过账后扣减库存，生成库存变动流水和审计记录。
- 报工：
  - 从工单详情发起报工，记录合格数量、不良数量、报工日期、报工人和备注。
  - 报工过账后更新工单累计报工数量，不直接影响库存。
- 完工入库：
  - 从工单详情发起完工入库，生成入库草稿。
  - 入库过账后增加成品或半成品库存，生成库存变动流水和审计记录。
- 状态与追溯：
  - 工单状态覆盖草稿、已发布、生产中、已完成、已取消。
  - 工单详情展示 BOM 快照、领料记录、报工记录、完工入库记录和相关库存流水。
  - 库存流水可反查来源生产单据、工单和 BOM 快照。
- 权限控制：
  - 生产管理菜单、生产工单、领料、报工、完工入库的查看、创建、更新、过账和状态动作权限。
  - 后端接口鉴权是最终安全边界，前端按钮控制只作为体验控制。
- 自动化测试、本地部署、浏览器功能验收和视觉分析。

### 本次不包含

- 高级排产、产能计划、设备负荷。
- 工艺路线、工序级派工、工序流转卡。
- 批次、序列号、库位、条码。
- 替代料、倒冲领料、退料、补料、返工、拆解、委外加工。
- 采购、销售联动触发生产。
- 库存占用锁定、缺料分析、齐套分析。
- 多单位换算、多组织、多公司、多租户。
- 审批流、附件图纸、工程变更。
- 正式成本核算、标准成本、移动加权、成本差异分析。

## 角色分工

- 产品经理：确认生产执行目标、状态流、数量规则、权限角色、异常场景和验收路径。
- UI 设计师：确认工单列表、表单、详情、领料、报工、完工入库、权限差异、异常状态和视觉验收标准。
- 前端开发：实现生产 API 封装、路由、页面组件、状态标签、权限展示、错误反馈、响应式适配和前端测试。
- 后端开发：实现数据库迁移、接口、状态机、BOM 快照、库存过账、事务一致性、权限、审计和后端测试。
- 测试：验证功能、接口、权限、异常、库存一致性、本地部署和浏览器视觉验收。
- 主代理：统筹任务边界、子代理讨论、文档基线、实施节奏、验证结果和阶段交付判断。

## 预期成果

- 文档成果：
  - 生产执行基础任务文档。
  - 生产执行基础设计规格。
  - 生产执行基础接口契约。
  - 生产执行基础测试计划。
  - 后续实施计划。
- 代码成果：
  - 生产工单、工单用料快照、领料单、报工单、完工入库单表结构。
  - 生产后端接口、状态流、事务过账、库存流水追溯、权限、审计和自动化测试。
  - 生产前端 API、路由、菜单入口、列表、详情、表单、领料、报工、完工入库、权限展示、错误反馈和自动化测试。
- 验收成果：
  - 管理员可在浏览器完成工单创建、发布、领料、报工、完工入库和追溯。
  - 仓库人员、生产人员、只读用户和无权限用户的权限表现与后端鉴权一致。
  - 库存不足、BOM 不可用、停用物料、超领、超报、超入、重复过账等异常有明确反馈。
  - 关键页面、表单、列表、详情、权限差异、异常和空状态具备截图证据与视觉分析结论。

## 验收标准

- 功能验收：
  - 管理员可创建生产工单草稿并发布。
  - 发布工单必须引用启用 BOM，并生成 BOM 用料快照。
  - 已发布工单可创建领料单、报工单和完工入库单。
  - 领料、报工、完工入库支持多次执行，但累计数量不得超过规则上限。
  - 工单达到完成条件后可完成；无已过账业务的工单可取消。
- 数据验收：
  - 所有生产数量使用 `NUMERIC(18,6)` 口径。
  - 领料过账后原材料或半成品库存减少，完工入库过账后产成品或半成品库存增加。
  - 报工只更新生产结果，不直接修改库存。
  - 库存余额、库存流水和生产来源单据一致。
  - BOM 快照可追溯，后续 BOM 修改不影响已发布工单。
- 权限验收：
  - 管理员具备生产执行全量权限。
  - 生产管理人员可创建、编辑草稿、发布、完成和取消工单，可查看执行记录。
  - 仓库人员可查看已发布工单，可创建、编辑和过账领料单、完工入库单。
  - 生产人员可查看已发布工单，可创建和过账报工单。
  - 只读用户只能查看工单和追溯信息。
  - 无权限用户不能访问生产页面或接口。
- UI 验收：
  - 页面符合后台 ERP 信息密度和现有设计风格。
  - 工单列表、详情页和明细表格的状态、数量和操作可扫描。
  - 操作列预留稳定宽度，关键按钮不可被截断。
  - 表单错误、库存不足、BOM 不可用、只读状态和无权限状态清晰可见。
  - 窄屏下筛选区、表格、表单和底部操作区不重叠；如需横向滚动，关键操作可触达。
- 测试验收：
  - 后端集成测试覆盖迁移、权限、工单发布、领料过账、报工过账、完工入库过账、异常、并发和审计。
  - 前端测试覆盖路由、权限、列表、表单、详情、动态明细、错误反馈和窄屏关键状态。
  - 全量后端测试、前端测试和前端构建通过。
- 部署验收：
  - 本地 PostgreSQL、后端和前端可启动。
  - 浏览器可以访问生产管理核心页面。
  - 健康检查和核心验收路径通过。
- 浏览器视觉分析：
  - 视觉分析记录保存到 `docs/testing/production-execution-visual-audit/`。
  - 覆盖工单列表、查询结果、空状态、新建表单、详情、领料、报工、完工入库、错误状态、只读权限和窄屏视口。

## 风险与处理

- BOM 修改影响已发布工单：发布时生成 BOM 快照，工单详情读取快照，不回读可变 BOM 明细。
- 库存被扣成负数：领料过账时锁定库存余额并校验过账后数量不小于 0。
- 重复提交导致重复扣减或入库：来源明细唯一约束、单据状态和事务校验共同防重。
- 超领、超报、超入破坏生产数据可信度：一期全部禁止，返回明确业务错误。
- 工单状态不清导致按钮错显：后端状态机定义最终可操作性，前端只做展示和体验保护。
- 详情页信息过密：采用状态摘要加分区表格，不使用嵌套卡片堆叠。
- 窄屏侧栏占用空间：若不影响核心访问和操作，记录为后续优化；若导致关键操作不可见或表单不可提交，视为阻断缺陷。

## 变更控制

- 变更原因：仓库与库存基础能力已完成并进入主分支验收，下一里程碑需要打通生产执行闭环。
- 影响范围：新增生产执行文档、后续生产后端和前端实现、库存流水类型扩展、测试与浏览器验收路径。
- 涉及文件：
  - `docs/product/product-decisions.md`
  - `docs/tasks/008-production-execution-foundation.md`
  - `docs/api/production-execution-api.md`
  - `docs/testing/production-execution-test-plan.md`
  - `docs/superpowers/specs/2026-07-03-production-execution-design.md`
- 验收影响：后续阶段验收将新增生产工单、领料、报工、完工入库、库存追溯和视觉分析要求。
- 决策依据：一期范围、核心业务流程、产品决策记录、库存阶段验收结果和五类子代理讨论结论。

## 缺陷与阻断

- 阻断缺陷：服务无法启动、核心页面无法访问、工单无法发布、BOM 快照缺失、领料后库存未扣减、完工入库后库存未增加、库存可为负、余额和流水不一致、无权限用户可写生产接口、写操作无审计、浏览器视觉分析缺失。
- 严重缺陷：数量精度错误、超领超报超入未拦截、已过账单据可编辑、状态按钮与后端规则不一致、详情追溯不完整、只读权限前端展示不一致但后端能拦截。
- 已知问题：排产、工艺路线、批次、序列号、库位、条码、倒冲、退料、返工、委外、成本核算、采购销售联动不在本阶段。
- 是否允许进入验收：必须完成任务文档、接口契约、设计规格、实施计划、自动化测试、本地部署、浏览器验收和视觉分析后再判断。

## 执行记录

- 计划：先完成生产执行基础文档基线，再创建实施计划和功能分支实施。
- 变更：2026-07-03 基于五个子代理讨论结果确认为第五个业务模块。
- 验证：文档基线提交前执行占位扫描、范围排除项检查和 `git diff --check`。
- 视觉分析：本任务不启动服务，视觉分析在阶段浏览器验收任务中执行并保存截图证据到 `docs/testing/production-execution-visual-audit/`。
- 结论：文档基线完成后，后续任务以本任务文档、接口契约、测试计划和设计规格作为实施与验收依据。

### 2026-07-03 生产执行任务 7 自动化验证记录

- 后端全量测试：按计划使用 Docker 命令执行 `docker run --rm -e TESTCONTAINERS_RYUK_DISABLED=true -e TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal -v "${PWD}:/workspace" -v /var/run/docker.sock:/var/run/docker.sock -w /workspace/apps/api maven:3.9-eclipse-temurin-21 mvn -q test`，退出码为 0；未使用本地 Java 21 替代。Surefire 报告汇总为 13 个测试套件、84 个用例、0 失败、0 错误、0 跳过。
- Testcontainers 遗留容器检查：执行 `docker ps -a --filter "label=org.testcontainers" --format "{{.ID}} {{.Image}} {{.Status}}"`，退出码为 0，无输出，未发现遗留 Testcontainers 容器。
- 前端全量测试：在 `apps/web` 执行 `npm test`，退出码为 0，27 个测试文件、166 个用例通过。
- 前端构建：在 `apps/web` 执行 `npm run build`，退出码为 0，`vue-tsc --noEmit && vite build` 成功，Vite 构建完成。
- 空白检查：在仓库根目录执行 `git diff --check`，退出码为 0；验证执行前无输出，文档更新后复查仅出现两个文档的 LF/CRLF 工作区提示，无空白错误。
- 边界说明：本记录只覆盖任务 7 自动化验证与代码质量检查；本地部署、浏览器功能验收和视觉分析尚未进入，后续按任务 8/9 执行并另行记录。

### 2026-07-03 生产执行任务 8 本地部署和浏览器功能验收记录

- 本地 PostgreSQL：执行 `docker ps --filter "name=qherp-postgres" --format "{{.Names}} {{.Status}} {{.Ports}}"` 初始无运行容器；按本地开发说明执行 `docker compose up -d postgres` 后确认 `qherp-postgres` 为 `healthy`，端口为 `15432->5432`。
- 后端服务：执行指定 Docker 命令启动 `qherp-api-local`，日志出现 `Started QherpApiApplication`；健康检查 `Invoke-RestMethod http://127.0.0.1:18080/api/health` 返回 `{"service":"qherp-api","status":"UP"}`。后端地址为 `http://127.0.0.1:18080`。
- 前端服务：在 `apps/web` 执行 `npx vite --host 127.0.0.1`，Vite 监听 `http://127.0.0.1:5173/`，浏览器访问返回 HTTP 200。
- 验收数据准备：复用已有启用 BOM `BVA171IK-BOM-FG-A-V11`，创建任务 8 浏览器验收原料仓 `任务8浏览器原料仓20260703134836` 和成品仓 `任务8浏览器成品仓20260703134836`；通过库存期初单 `INV-OPEN-20260703054836223-000` 给 `BVA171IK-RM-X`、`BVA171IK-SF-B`、`BVA171IK-AUX-Z` 各开账 1000。
- 管理员主路径：使用 `admin / Qherp@2026!` 在浏览器创建工单 `MFG-WO-20260703055314097-000`，发布后生成 BOM 用料快照；创建并过账领料单 `MFG-ISS-20260703055813114-000`、报工单 `MFG-RPT-20260703055813727-000`、完工入库单 `MFG-RCP-20260703055814165-000`，随后完成工单。最终工单状态为 `COMPLETED`，计划数量 4，累计报工 4，合格 4，累计入库 4。
- 库存结果：原料仓余额从 1000 扣减为 `BVA171IK-RM-X=989.8`、`BVA171IK-SF-B=996`、`BVA171IK-AUX-Z=988`；成品仓 `BVA171IK-FG-A` 增加到 4。后端库存流水接口返回 `PRODUCTION_ISSUE` 三条和 `PRODUCTION_RECEIPT` 一条，来源类型分别为 `PRODUCTION_MATERIAL_ISSUE` 和 `PRODUCTION_COMPLETION_RECEIPT`。
- 异常验收：库存不足领料在浏览器过账时返回 409，页面显示“生产领料库存不足”；超领、超报、超入表单分别显示“本次领料不能大于未领数量”“累计报工不能超过计划数量”“入库数量不能超过累计合格报工减已入库数量”；停用 BOM 请求返回 `PRODUCTION_BOM_INVALID`，停用仓库请求返回 `PRODUCTION_WAREHOUSE_INVALID`。
- 停用物料补验：2026-07-03 14:35:21 +08:00 使用接口构造与验证。原因是生产工单页面只加载启用物料、启用 BOM 和启用仓库，普通浏览器路径无法直接选择停用物料；本轮用管理员接口创建独立成品 `T8DMAT-FG-20260703143520`、子项物料 `T8DMAT-RM-20260703143520` 和启用 BOM `T8DMAT-BOM-20260703143520`，随后停用子项物料并发布引用该 BOM 的草稿工单。请求 `PUT /api/admin/production/work-orders/5/release` 返回 HTTP 400，错误码 `PRODUCTION_MATERIAL_INVALID`，错误消息“生产用料物料不存在或已停用”，失败后工单仍为 `DRAFT`。结论：停用物料参与生产发布被受控拒绝，任务 8 停用物料异常覆盖已补齐。
- 权限验收：创建只读用户 `t8_read_20260703135941`，浏览器可查看草稿工单详情但仅显示“退出”按钮，无编辑、发布、领料、报工、完工入库、完成、取消和过账按钮；只读用户直接调用发布接口返回 403 `AUTH_FORBIDDEN`。创建无权限用户 `t8_noperm_20260703135941`，浏览器访问生产工单跳转 `/forbidden?from=/production/work-orders/2`，接口读取工单返回 403 `AUTH_FORBIDDEN`。
- 生产角色权限补验：2026-07-03 14:44:42 +08:00 使用浏览器和接口共同验证独立工单 `MFG-WO-20260703064254897-001`。仓库人员 `t8_wh_20260703144254` 访问 `/production/work-orders/6` 成功，页面标题为“生产工单详情”，顶部只显示“领料”“完工入库”，领料草稿和完工入库草稿行显示“过账”按钮，不显示“发布”“报工”；接口创建并过账领料单 `MFG-ISS-20260703064255200-000` 和完工入库单 `MFG-RCP-20260703064255370-000` 均成功且状态为 `POSTED`，直接调用 `PUT /api/admin/production/work-orders/6/release` 返回 HTTP 403、错误码 `AUTH_FORBIDDEN`。生产人员 `t8_prod_20260703144254` 访问同一工单详情成功，顶部只显示“报工”，报工草稿行显示“过账”按钮，不显示“领料”“完工入库”“发布”；接口创建并过账报工单 `MFG-RPT-20260703064255309-000` 成功且状态为 `POSTED`，直接调用领料过账 `PUT /api/admin/production/work-orders/6/material-issues/5/post` 和完工入库过账 `PUT /api/admin/production/work-orders/6/completion-receipts/3/post` 均返回 HTTP 403、错误码 `AUTH_FORBIDDEN`。结论：仓库人员和生产人员的页面权限表现、允许操作和后端越权拒绝符合测试计划。
- 未完全通过项：库存流水页面 `/inventory/movements` 可看到生产领料和完工入库流水的仓库、物料、方向、数量和原因，但“变动类型”列未显示 `PRODUCTION_ISSUE`、`PRODUCTION_RECEIPT` 或对应中文标签；后端接口和生产工单详情接口已返回正确类型。该项未写作通过，需前端补齐库存流水生产类型展示后复验。
- 复验记录：提交 `9572c9f` 补齐库存流水生产变动类型显示，提交 `b00281b` 补强生产类型表格列测试后，本轮重启前端并以实际地址 `http://127.0.0.1:5174/` 浏览器访问 `/inventory/movements`；后端 `http://127.0.0.1:18080` 健康检查为 `status=UP`，PostgreSQL `qherp-postgres` 为 `healthy`。管理员登录后按关键词 `BVA171IK` 查询，页面表格中三条生产领料流水的“变动类型”列显示“生产领料”，完工入库流水的“变动类型”列显示“完工入库”；仓库、物料、方向、数量和原因仍可见。原任务 8 未完全通过项已复验通过。
- 边界说明：本记录覆盖任务 8 本地部署和浏览器功能验收；视觉分析和截图证据尚未执行，属于后续任务 9。
