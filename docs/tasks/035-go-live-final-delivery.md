# 035 上线准备与最终生产交付

> 本文件是合并后 035 的唯一权威阶段说明，同时承载目标、范围、环境、工作包、验收矩阵、阻断与非阻断问题、执行记录和最终交付证据。原 036 已正式废止，不保留占位阶段。

## 阶段目标

在 000—034 已交付业务能力基础上，不新增业务功能，完成可重复生产部署、安全与权限加固、性能容量验证、PostgreSQL/MinIO 联合备份恢复、V34→V36 与空库迁移演练、运行监控和运维基线；随后冻结唯一上线候选，在同一候选、同一生产式环境和同一连贯数据集上完成正向、反向、权限、业务月结、总账、财务结账、固定经营分析和真实桌面最终验收，形成完整生产交付包。

## 阶段状态

- 状态：执行中；固定测试角色已完成全阶段代码评审及全部整改差异复审，最终结论为阻断 0、严重 0、一般 0；最终候选镜像和五容器拓扑已启动，密钥边界与 API 单容器重启实测通过，等待进入唯一全量验证窗口。
- 日期：2026-07-22。
- 用户授权：因本阶段不新增业务，用户授权主代理独立自主推进；实施中阻断问题立即修复，非阻断问题先统一登记，在两个窗口计划内项目完成后集中修复。若涉及代码修改，必须复用固定测试角色进行代码评审。
- 权威工作区：`F:\zhangqiao\AI-study\qherp-035-go-live-final-delivery`。
- 权威分支：`codex/035-go-live-final-delivery`。
- 启动基线：`main@64f33e664a2765e5bee824ae2b503c9d4955920e`，其中包含已确认的合并设计与实施计划；`origin/main@382861e3b9beedb78ee88b90613845cad5e8872c`。
- 当前路线：000—034 已交付；035 是唯一剩余最终阶段；原 036 已废止。

## 用户决策与执行例外

- 035 不进行五角色业务目标讨论，不新增产品、UI、前端或后端业务工作包；主代理直接处理上线治理、配置、部署工具、验证和交付资料。
- 本例外不允许主代理静默改变业务规则、状态机、接口语义、会计口径、迁移含义或数据边界。
- 任何代码、脚本、Dockerfile、Nginx、生产配置或前端构建治理修改，均视为需要测试角色代码评审的修改；固定测试角色只审查，不参与实现。
- 若发现必须新增业务能力或改变业务契约才能上线，立即停止作为普通 035 整改，记录影响并建立新的业务阶段或提交用户决策。

## 全局范围与禁止项

### 纳入范围

- 完整生产式 Compose 编排：PostgreSQL、MinIO、API、Web/Nginx、健康检查、重启、日志轮转、资源限制，并只由 Web/Nginx 在本机发布唯一入口。
- 生产配置与密钥：生产 profile、必填环境变量、DPAPI 本机密钥载体、管理员/数据库/对象存储凭据轮换、仓库明文扫描。
- 安全：权限矩阵、职责分离、登录限流、会话 cookie、CSRF、安全响应头、敏感字段、禁用账号、越权和常见 Web 基线检查。
- 供应链：前后端依赖审计、生产镜像与发布物清单、摘要和来源提交。
- 性能与容量：健康、登录、认证只读、固定报表和批量任务的本机生产式基线。
- 数据：空库 V1→V36、V34 副本→V36、034 V36 全事实副本三类验证。
- 备份恢复：停止写入口后的 PostgreSQL custom dump、MinIO 镜像、清单、摘要、恢复到新资源、恢复后完整验证。
- 运维：部署、启动、停止、重启、健康、日志、备份、恢复、故障处理、回滚和验收说明。
- 最终验证：正向、反向、权限、月结与财务闭环、固定经营分析、真实桌面、交付包和候选哈希。
- 034 明确转入后续治理的六项一般问题。

### 明确禁止

- 固定资产、工资薪酬、多币种、正式收入确认、法定三大报表、完整税务申报、完整红字发票、APS、MES、完整 QMS、WMS/PDA 等新业务能力。
- 为通过验收降低权限、安全、性能、数据一致性、迁移或恢复标准。
- 把阶段性测试、接口测试、代码阅读或旧阶段证据替代冻结候选上的最终验证。
- 修改 V1—V36 已存在迁移或 checksum；035 原则上不新增业务迁移。
- 在仓库、Git、命令行、日志或交接中写入真实明文凭据。
- 直接在 034 验收数据库和 bucket 上执行破坏性迁移、恢复、压测写入或最终候选操作。

## 环境与候选设计

- Compose 项目：`qherp035`。
- 最终验收入口：`http://127.0.0.1:45173/`。
- API 健康入口：经 Web 反向代理访问 `http://127.0.0.1:45173/api/health`；API 不发布宿主端口。
- PostgreSQL：Compose 内部地址 `postgres:5432`，候选数据库 `qherp_035_candidate`，不发布宿主端口。
- MinIO：Compose 内部地址 `minio:9000/9001`，候选 bucket `qherp-035-candidate`，不发布宿主端口。
- 本机 DPAPI 密钥载体：`C:\Users\14567\.codex\secrets\qherp\035\runtime-secrets.clixml`；只允许当前 Windows 身份读取，不进入仓库。
- 备份与验证结果：`C:\Users\14567\.codex\backups\qherp\035-*`；不进入仓库。
- 生产编排只允许 Web/Nginx 绑定 `127.0.0.1:45173`；API、PostgreSQL 和 MinIO 仅在 Compose 内部网络可达。非本机访问必须在受控 TLS 入口后提供服务，并将生产会话 cookie 切换为 `Secure`；本阶段不伪造企业域名或 CA 证书。
- 同一编排必须支持：空库初始化、V34 副本升级、V36 全事实候选恢复，且所有资源名、端口、卷和 bucket 可参数化隔离。

## 性能、恢复和稳定性指标

### 性能

- 健康接口：并发 20、请求 1000，HTTP 非 2xx 为 0，P95 不高于 250ms。
- 有效登录：顺序 20 次，成功率 100%，P95 不高于 1500ms；无效登录突发必须触发限流且不得建立会话。
- 认证只读接口：并发 20、请求 1000，非预期错误率不高于 0.1%，5xx 为 0，P95 不高于 1000ms。
- 固定经营报表：并发 10、请求 300，5xx 为 0，P95 不高于 3000ms，不允许分页汇总、金额或权限语义变化。
- 压测只使用候选副本和只读请求，不向 034 或默认正式资源写入。

### 备份恢复

- 当前约 30MB 数据规模下，联合备份不高于 10 分钟，恢复到新资源不高于 15 分钟。
- 生产运行目标 RPO 为最近一次成功联合备份，默认日备份即不高于 24 小时；目标 RTO 不高于 30 分钟。
- 备份必须包含 PostgreSQL custom dump、可读清单、MinIO 全量对象、对象清单、SHA256、来源提交、迁移版本和开始/结束时间。
- 恢复必须使用新数据库、新 bucket 或新卷，不覆盖来源；恢复后必须验证 V29—V36 checksum、失败迁移 0、数据库 `AVAILABLE` 与 MinIO 对象精确一致、关键事实计数和登录健康。

### 稳定性

- 整栈从停止到健康不高于 180 秒。
- API、Web、PostgreSQL 和 MinIO 均有健康检查与自动重启；日志配置轮转上限，禁止无限增长。
- API 内存上限 1GiB，Web 256MiB，PostgreSQL 1GiB，MinIO 512MiB；指标不满足时以实测为准调整并记录原因。

## 两个顺序窗口

### 窗口一：上线准备与候选冻结

1. 完成生产编排、生产配置、密钥、权限、安全和依赖检查。
2. 完成空库、V34→V36、V36 全事实三类迁移/恢复验证。
3. 完成联合备份恢复、性能容量、重启健康、日志监控和故障处置验证。
4. 即时修复全部阻断和严重问题；非阻断问题持续登记，不在单项检查中反复打断窗口。
5. 窗口计划内项目全部执行并汇总后，冻结唯一候选提交、镜像/发布物摘要、配置清单、迁移结果和候选数据指纹。

窗口一未通过不得进入窗口二。

### 窗口二：最终生产交付验证

1. 正向链：项目/合同→销售订单→缺料→采购/生产/外协→库存→发货→发票/收付款/费用→项目成本→业务月结→凭证/总账→财务结账→经营分析。
2. 反向链：销售/采购/生产反向、往来冲减或退款、凭证冲销、业务月结重开、财务反结账和来源变化后的重新处理。
3. 权限链：超级管理员、业务、仓储、生产、财务、审批、只读、无成本和禁用账号；双人审批不得自批。
4. 真实桌面：只检查本阶段新增/修改/受影响页面和全链路关键入口；不截图、不测试移动端，控制台错误和页面级溢出为 0。
5. 完整交付包：部署、密钥、备份、恢复、迁移、权限、性能、安全、运维、验收、已知问题和候选清单。

窗口二原则上不开发。发现阻断或严重问题时解除候选冻结、立即修复、只复验受影响路径并重新冻结；涉及业务契约变化时按上文停止规则处理。

## 工作包

### 工作包一：生产编排与安全基线

- API 与 Web 多阶段生产镜像、完整 Compose、生产 profile、Nginx 反向代理和限流、安全响应头、健康检查、日志轮转、资源限制。
- DPAPI 密钥初始化与受控启动入口，所有生产密钥必填，禁止开发默认值回落。
- 权限矩阵、职责分离、管理员轮换、敏感字段和安全基线验证。

### 工作包二：数据、恢复、迁移与性能

- 联合备份、恢复、清单和验证工具。
- 空库、V34→V36、V36 全事实三类演练。
- 可重复的认证只读性能工具和固定阈值结果。
- 依赖、镜像、仓库明文和发布物摘要检查。

### 工作包三：非阻断集中整改与最终交付

- 集中修复 034 转入的六项一般问题及 035 新发现的一般问题。
- 运行受影响测试、代码评审和差异复验。
- 冻结候选并完成窗口二、全量验证、交付包、交接、合入、推送和正式主分支服务切换。

## 验收矩阵

| 编号 | 验收项 | 完成条件 |
|---|---|---|
| A01 | 路线与隔离 | 020—035 路线唯一，035 分支/工作树/基线正确，034 与主工作区不受影响 |
| A02 | 生产镜像 | API、Web 镜像可重复构建，版本、来源提交和 SHA256 可追溯 |
| A03 | 完整编排 | PostgreSQL、MinIO、API、Web 同一 Compose 启停、健康和重启通过 |
| A04 | 密钥 | 生产密钥全部必填、DPAPI 保存、仓库明文 0、管理员凭据已轮换 |
| A05 | 安全 | 登录限流、会话、CSRF、安全头、禁用账号、越权和敏感字段通过 |
| A06 | 权限矩阵 | 507 个当前权限与角色映射可导出、可复核，双人职责分离通过 |
| A07 | 依赖与镜像 | 前后端依赖、最终镜像和发布物完成高严重度检查并有结论 |
| A08 | 空库迁移 | 空库自然迁移至 V36、checksum 精确、失败迁移 0、初始管理员可登录 |
| A09 | V34 升级 | V34 只读副本自然前迁 V35/V36，历史事实、文件和 checksum 保持 |
| A10 | 全事实候选 | 034 V36 全事实以备份恢复到 035 新资源，来源不受写入 |
| A11 | 联合备份 | PostgreSQL、MinIO、清单、摘要和来源元数据完整，耗时达标 |
| A12 | 联合恢复 | 新资源恢复成功，V36、关键事实、对象一致、登录健康，耗时达标 |
| A13 | 性能 | 四组性能指标全部通过，5xx 为 0，业务金额和权限语义不变 |
| A14 | 稳定性 | 整栈 180 秒内健康，重启、日志轮转、资源限制和故障恢复通过 |
| A15 | 候选冻结 | 提交、镜像/发布物、配置、数据指纹和验证结果形成唯一清单 |
| A16 | 正向链 | 完整项目型制造正向业财链在同一候选与数据集通过 |
| A17 | 反向链 | 反向业务、冲销、重开、反结账和来源变化路径通过 |
| A18 | 权限链 | 九类账号、后端鉴权、前端路由和职责分离通过 |
| A19 | 月结财务 | 030 CLOSED、031 POSTED/账簿/试算、032 损益/银行/税务/关闭闭环通过 |
| A20 | 分析追溯 | 033 七类固定分析、LIVE/FROZEN、来源追溯和权限脱敏通过 |
| A21 | 桌面 | 真实桌面关键入口可访问，控制台/页面异常/溢出为 0，规范适用项通过 |
| A22 | 非阻断整改 | 034 转入和 035 新登记的一般问题集中修复并完成差异复验 |
| A23 | 代码评审 | 如有代码修改，固定测试角色完成代码评审，阻断/严重意见闭合 |
| A24 | 全量验证 | 后端、前端、类型检查、生产构建、迁移、验证器、安全、性能、恢复和桌面统一通过 |
| A25 | 交付包 | 部署、密钥、权限、备份恢复、迁移、性能、安全、运维和验收材料完整 |
| A26 | 正式交付 | 功能分支合入推送，远端哈希一致，主分支候选服务健康且可浏览器验收 |

## 问题登记与处理规则

### 阻断问题

| 编号 | 问题 | 当前状态 | 处理要求与证据 |
|---|---|---|---|
| B01 | 仓库只有 PostgreSQL/MinIO 开发 Compose，没有 API/Web 完整生产编排 | RESOLVED | 已建立四个业务服务加无网络密钥保活服务的固定镜像生产 Compose；四个业务服务实际健康 |
| B02 | 应用、数据库、对象存储和初始管理员允许开发默认凭据回落，034 管理员仍为开发默认口令 | RESOLVED | 生产变量全部必填，DPAPI 文件仅当前 Windows 身份可读，035 管理员已轮换并实际登录成功 |
| B03 | 没有独立生产联合备份、恢复、RPO/RTO 和恢复后完整验证 | RESOLVED | 联合备份 2.829 秒、034 全事实恢复 38.11 秒；清单、逐文件/对象 SHA256、41/41 和恢复后登录健康通过 |
| B04 | 没有空库、正式 V34 副本和 V36 全事实三类统一迁移/恢复演练 | RESOLVED | 空库 24.806 秒到 V36、正式只读克隆 V34 26.054 秒到 V36、034 V36 全事实恢复到 035，来源均未写入 |
| B05 | 没有可重复性能工具、阈值和生产式容量证据 | RESOLVED | 已建立固定只读压测工具；健康、登录、认证读取和固定经营报表全部通过门槛 |
| B06 | 没有冻结 035 候选上的统一正向、反向、权限、月结财务和交付证据 | OPEN | 窗口一冻结后由窗口二闭合 |
| B07 | 宿主默认 `java` 为 1.8，不能作为 Java 21 生产运行基线 | RESOLVED | API 固定使用摘要锁定的 Temurin 21 镜像，运行态实测 Java 21.0.11 |
| B08 | 首次 Web 候选引用官方 Nginx 镜像中不存在的 `/etc/nginx/proxy_params`，Web 无法启动 | RESOLVED | 增加失败契约后改为显式代理头，重建后 Web 健康 |
| B09 | 局部缓存 `add_header` 覆盖安全头继承，真实主页缺失 CSP 等响应头 | RESOLVED | 统一缓存变量与服务级响应头；真实主页已返回六类安全/缓存头 |
| B10 | 登录限流初值 5 次/分钟与 20 次合法顺序登录验收冲突 | RESOLVED | 调整为 10 次/秒、突发 20；保留无效登录突发限流能力，待性能工具统一复验 |
| B11 | 初版 API/Web 运行镜像和 JDBC 驱动存在可修复高严重度漏洞，旧 Web 基础镜像还含严重漏洞 | RESOLVED | JDBC 固定 42.7.12；API 改用 Temurin 21 Alpine 并更新基础包；Web 改用 Nginx 1.30.4 Alpine；实际成品复扫均为高危/严重 0，运行验证通过 |
| B12 | 034 来源备份脚本重启 API 时未恢复冻结交付元数据，健康 200 不能证明来源仍处于已验收状态 | RESOLVED | 固定恢复 034 环境、手册、演示数据版本/状态/时间并通过认证接口精确核对；重新备份 `C:\Users\14567\.codex\backups\qherp\035-v36-fullfacts-20260722120453`，Stage034FullFacts 211/211、V36、对象 41/41、API/代理 200 |
| B13 | DPAPI 解密值直接进入容器 `environment`，可由 Docker 配置元数据读取，不满足密钥长期载体边界 | RESOLVED | PostgreSQL/MinIO 使用文件型 secret；API 由标准输入写入共享内存型 configtree 卷；最终五容器 `Config.Env` 与四类真实密钥值匹配 0，API 文件均为 `0600`、UID/GID `100:101` |
| B14 | Docker Compose 2.37 不允许只读 API 挂载环境来源 secret；改用 API 私有 tmpfs 后又会在容器重启时丢失密钥，破坏自动恢复 | RESOLVED | 无网络密钥保活服务持续挂载 Docker local tmpfs 卷，API 只读共享；最终候选 API 单容器重启 17.085 秒恢复健康，非空密钥文件保持 4/4，代理健康 200 |
| B15 | 上一次失败的 Compose recreate 留下仅为 `Created` 的 API 临时替换容器，占用重建状态并导致正式名称冲突 | RESOLVED | 核对容器 ID、项目/服务标签、镜像和状态后只删除该 `qherp035/api` 残留；重试后 API/Web 使用最终提交镜像创建并健康 |
| B16 | 运行证据脚本在严格模式下直接读取无 healthcheck 保活容器不存在的 `State.Health` 属性 | RESOLVED | 先以自测锁定失败，再改为 `PSObject.Properties["Health"]` 可选读取；生产自测、AST 解析和测试角色差异复审均为 0 问题 |

### 非阻断问题

| 编号 | 来源 | 问题 | 状态 |
|---|---|---|---|
| N01 | 034 | 历史导入带 `adapterCode` 时未自动预置适配器筛选 | RESOLVED，从 URL 初始化筛选并覆盖深链定向测试 |
| N02 | 034 | 平台与部分主数据页面仍有旧式 inline 查询区 | RESOLVED，平台 5 页和主数据 4 页改为顶部标签标准查询区，受影响测试已锁定 |
| N03 | 034 | 批量状态抽屉候选较多时底部动作依赖滚动到达 | RESOLVED，内容区独立滚动且 footer 固定保留，定向测试通过 |
| N04 | 034 | FullFacts 缺少部分稳定浏览器状态样本 | RESOLVED，补齐数据修复草稿/已取消、历史导入多页错误、审批真实待办稳定样本 |
| N05 | 034 | `rebuild-acceptance.ps1` 失败清理分支使用只读 `$PID` 变量名 | RESOLVED，循环变量改为 `$processId`，新增回归断言且 `demo-data-self-test` 通过 |
| N06 | 034 | 前端生产构建存在 Rollup 大 chunk 警告 | RESOLVED，Element Plus 深入口与依赖分包后最大 JS chunk 441.13 KiB，构建无大 chunk 或循环 chunk 告警 |
| N07 | 035 审计 | README 与产品决策曾停在 034 开发中 | RESOLVED，路线合并提交已修复 |
| N08 | 035 审计 | 当前 034 PostgreSQL 16、MinIO latest 匿名卷和宿主进程组合不可直接作为可重复生产拓扑 | RESOLVED，以只读联合备份迁入 035 固定摘要四服务拓扑，034 来源保持 V36/41/41 和健康 |
| N09 | 035 审计 | 当前数据规模约 30MB，性能结论只代表本机当前数据与合成并发 | CLOSED（适用范围已冻结），性能报告仅作为当前单机候选基线，不外推为未来容量承诺 |
| N10 | 035 构建 | `npm audit` 报告开发依赖链中 `brace-expansion@2.1.1` 1 个高严重度拒绝服务告警；生产依赖审计为 0 | RESOLVED，锁文件升级到 2.1.2，`npm ci` 后完整与生产依赖审计均为 0 |
| N11 | 035 安全核验 | 生产已关闭 OpenAPI/Swagger 且公共 Web 未代理对应路径，但认证后直连本机 API 的未知静态资源由全局异常处理返回 500 而非 404 | RESOLVED，通用缺失资源映射为标准 404 响应；`ApiResponseTests` 10 项通过，同时 API 取消宿主端口发布 |
| N12 | 035 供应链核验 | MinIO 开源镜像已停止维护，当前镜像扫描含仅在未启用的 OIDC/LDAP/复制/S3 Select 等路径或已受网络边界限制的高危/严重项；官方已修复版本转为需要许可证的 AIStor | MITIGATED，已取消全部宿主端口，只允许 Compose 内部 API 访问；未启用相关功能且凭据由 DPAPI 保护，待冻结候选运行复验后关闭，长期保留许可证/替代品决策 |
| N13 | 035 供应链核验 | 当前官方 PostgreSQL 18.4 镜像仍因启动辅助二进制 `gosu` 的未使用 Go/TLS 代码路径及 `c-ares` 报告高危/严重项 | MITIGATED，已取消数据库宿主端口，`gosu` 仅用于受控容器启动且不执行网络/TLS 路径；待冻结候选运行复验后关闭并保留官方镜像升级策略 |
| N14 | 035 代码评审 | 迁移演练把一次性数据库、管理员和 S3 密钥拼入 `docker run -e` 命令参数 | RESOLVED，改为当前用户 ACL 保护的临时 secret 文件和只读 bind mount，容器/网络清理后校验路径边界再删除临时目录 |

新发现非阻断问题继续追加本表，不在窗口计划项执行过程中零散返工；窗口计划内项目完成后统一去重、集中修复和差异复验。

## 变更记录

| 日期 | 变更 | 原因与影响 | 决策 |
|---|---|---|---|
| 2026-07-22 | 原 035、036 合并 | 两阶段都服务同一最终生产交付，不新增业务；拆分会重复治理，完全混合又会失去候选冻结点 | 保留 035，内部两个顺序窗口；原 036 废止 |
| 2026-07-22 | 主代理独立实施 | 用户明确要求因无新增业务由主代理自主推进 | 主代理直接实施；代码修改由固定测试角色评审 |
| 2026-07-22 | 问题处理顺序 | 用户要求阻断立即修复，非阻断先登记后集中处理 | 按阻断表与非阻断表执行，不降低完成标准 |
| 2026-07-22 | 收紧生产网络边界 | MinIO 开源版与 PostgreSQL 官方镜像存在无法通过无许可证替换直接消除的供应链残余告警 | 只保留 Web 的 `127.0.0.1:45173` 宿主入口；API、PostgreSQL、MinIO 取消端口发布，以内部网络隔离作为当前候选的强制补偿控制 |
| 2026-07-22 | API 密钥改为共享内存保活 | Compose 环境来源 secret 与只读 API 不兼容；容器私有 tmpfs 经真实 `docker restart` 证明会丢失内容 | 使用无网络保活容器持续挂载本机 tmpfs 卷，API 只读共享；统一启动、重启和备份恢复路径从 DPAPI 经标准输入重注入，测试角色最终复审 0/0/0 |

## 当前基线证据

- 034 入口 `35173`、Web `35174`、API `38080`，以及 033 入口 `25173`、API `28080` 均为 HTTP 200。
- 034 PostgreSQL 约 30MB，Flyway 最新成功 V36，V29—V36 checksum 与 034 交付记录一致；失败迁移 0。
- 034 数据库 `AVAILABLE` 文件 41，交付记录中的 MinIO 对象为 41；`sys_permission` 507、角色 7、用户 11。
- 034 PostgreSQL 使用 `postgres:16-alpine`，MinIO 使用 `minio/minio:latest`，两者均为匿名 Docker volume；API/Web/代理运行在宿主进程。
- 宿主默认 `java` 为 1.8；可用显式 JDK 21 位于 `C:\Users\14567\.jdks\temurin-21-extract\jdk-21.0.11+10` 等目录；Node 22.17.0、npm 10.9.2、Docker 28.2.2 可用。
- 仓库当前没有 API/Web Dockerfile、生产 Compose、CI 工作流、独立生产恢复工具或性能压测工具。
- 启动审计时的安全基础具备 Spring Security、会话、CSRF、BCrypt 和后端权限拦截，但生产 cookie、登录限流、生产必填密钥和完整安全响应头尚未冻结；这些缺口现已由工作包一闭合并进入后续统一复验。

## 执行记录

### 2026-07-22 工作包一：生产编排与安全基线

- 以失败自测起步，补齐 API/Web 多阶段镜像、四服务生产 Compose、production profile、Nginx 代理与限流、DPAPI 密钥初始化和统一启动入口；生产基线自测已通过。
- 首次空库由 Flyway 自然执行 V1—V36，最新记录 `36|success=true`、失败迁移 0；空库生成 1 个启用的 `admin`，使用用户指定的新口令登录成功，角色 1、有效权限 456。
- `qherp035-postgres-1`、`qherp035-minio-1`、`qherp035-api-1`、`qherp035-web-1` 已在独立端口真实健康；主页与健康接口均为 HTTP 200。
- API 以非 root 用户 `qherp`、Java 21.0.11、只读根文件系统和 1GiB 内存上限运行；Web 使用只读根文件系统和 256MiB 内存上限；四服务均为 `unless-stopped`、JSON 日志轮转和固定镜像版本/摘要。
- 生产密钥文件位于既定 DPAPI 路径，已关闭 ACL 继承，仅当前 Windows 身份拥有一条完全控制规则；无密钥运行 Compose 配置失败，无生产变量运行 API 失败，不存在开发默认值回落。
- 真实主页已返回 CSP、`X-Frame-Options: DENY`、`nosniff`、Referrer-Policy、Permissions-Policy 与 `Cache-Control: no-store`；OpenAPI/Swagger 生产端点未启用，公共 Web 不代理对应路径。
- 构建确认前端生产产物仍有约 767.14KiB Rollup 大 chunk（N06）；完整 npm 审计只有开发依赖告警 1 个，`npm audit --omit=dev` 为 0（N10）。
- 本工作包已提交为 `ce2ae58ba8879f5ed14b553715955cd222caa5e3`，并以镜像标签 `035-ce2ae58ba887` 重建健康；后续集中整改还会产生最终候选提交和镜像摘要，故此处不提前冻结 A02—A08 最终结论。

### 2026-07-22 工作包二：联合备份、恢复与迁移演练

- 新增联合备份、034 来源停写适配、恢复、三类迁移演练和生产验证工具；自测覆盖清单必填字段、逐文件 SHA256、清单 SHA256、载荷篡改拒绝、来源覆盖拒绝、显式恢复确认和失败清理。
- 正式 `qherp` V34 Docker 数据卷保持停止，只以只读挂载克隆到一次性卷；克隆确认 V34 checksum `-629066235`，导出 custom dump 1,217,171 字节、SHA256 `690fa9431dc4db89dc28644efa6b0d81545da3d59c058563223dc77a5b1e4acd` 后销毁克隆容器和卷。
- 空库演练使用一次性 PostgreSQL 18、独立网络与当前 API 镜像，自然执行 V1—V36，耗时 24.806 秒；正式 V34 dump 前置确认 V34 后自然执行 V35/V36，耗时 26.054 秒。两者最新 V36、失败迁移 0，V29—V36 checksum 精确一致。
- 034 全事实备份前核对宿主 API PID/Java/JAR/端口后停止写入口；备份 `C:\Users\14567\.codex\backups\qherp\035-v36-fullfacts-20260722104715` 为 V36、41 个对象、耗时 2.258 秒。无论备份结果均按原 Java/JAR 与从容器/DPAPI 读取的参数恢复；034 新 PID 17060，API/代理健康 200，来源仍为 V36/41/41。
- 034 备份恢复到独立 `qherp_035_candidate` 与 `qherp-035-candidate`，基础设施 bucket 定位由来源名映射为目标名，不改变对象 key、内容、大小、SHA256 或业务关系；恢复耗时 38.11 秒，V36、失败迁移 0、数据库/MinIO 41/41、逐对象 SHA256、Web/API 健康和新管理员登录全部通过。
- 035 候选在停止 API/Web 写入口后另生成生产自身联合备份 `C:\Users\14567\.codex\backups\qherp\035-20260722-024837-ce2ae58b`，耗时 2.829 秒、41 个对象；生产验证 JSON 位于 `C:\Users\14567\.codex\backups\qherp\035-production-verify-20260722.json`。
- 以上均低于联合备份 10 分钟、恢复 15 分钟和 RTO 30 分钟门槛；正式 V34 卷、034 数据库/bucket、主工作区和当前 035 之外的资源未被迁移或覆盖。A08—A12 已形成运行证据，最终仍随冻结候选统一复验。

### 2026-07-22 工作包二续：性能、安全、权限与稳定性

- 固定性能报告位于 `C:\Users\14567\.codex\backups\qherp\035-performance-20260722-025603.json`：健康 1000 次/并发 20，P95 81.930ms；有效登录 20 次，P95 80.802ms；认证读取 1000 次/并发 20，P95 103.865ms；固定经营报表 300 次/并发 10，P95 419.121ms；5xx 为 0。无效登录 60 次中 39 次触发 429，其余为预期 401，均未建立会话，限流后合法登录恢复 200。
- 权限与运行证据位于 `C:\Users\14567\.codex\backups\qherp\035-runtime-20260722-032319`：Flyway V36、失败迁移 0、权限 507、角色 7、用户 11、文件 41；四容器健康，DPAPI ACL 仅当前 Windows 身份可读，证据导出不含容器环境变量和凭据。
- Trivy 使用官方镜像固定摘要 `sha256:bcc376de8d77cfe086a917230e818dc9f8528e3c852f7b1aff648949b6258d1c`。因官方 0.69.4—0.69.6 供应链事件，扫描器固定到官方公告确认安全的 0.69.3 摘要；报告保存在 `C:\Users\14567\.codex\backups\qherp\035-runtime-20260722-025659`。
- 初版扫描发现 PostgreSQL JDBC 42.7.11 高危项、API Ubuntu 基础镜像辅助二进制 7 个高危项、Web Debian 基础镜像 111 个高危/严重项。采用测试先行修复后，JDBC 固定 42.7.12，API 使用 Temurin 21 Alpine 并更新基础包，Web 使用 Nginx 1.30.4 Alpine；实际 `qherp/api:035-security-candidate` 与 `qherp/web:035-security-candidate` 复扫均为高危/严重 0。
- PostgreSQL 升级为官方 18.4 Alpine 3.24 固定摘要，原有候选卷原位重建后健康、V36 与 41/41 一致。官方镜像中仅剩启动辅助二进制和未触达库路径告警，按真实可达性登记 N13，不把扫描器总数误报为公共攻击面。
- MinIO 开源版扫描结果及官方 2026 安全公告已核对；官方修复版本属于需要许可证的 AIStor，不能在未获许可证和架构决策时静默替换。当前 MinIO 仅绑定本机、没有 OIDC/LDAP/复制/S3 Select 配置，API 容器是唯一持有对象存储凭据的应用方，残余风险登记 N12。
- 仓库全量 Trivy secret 扫描退出 0；生产 DPAPI 密钥不在仓库。项目长期开发/测试夹具与用户选择的新管理员口令字符串相同，但 035 实际生产载体仍只存在于 DPAPI 文件，不新增仓库明文。
- 单服务恢复实测：Web 7.11 秒、API 17.14 秒、PostgreSQL 6.81 秒、MinIO 6.88 秒，恢复后代理健康均为 200。停止 API 时主页保持 200，API 代理返回可诊断的 504；重新启动后恢复 200。
- 保留数据卷的整栈停止并冷启动到四服务健康耗时 33.544 秒，低于 180 秒门槛；冷启动后再次验证 V36、数据库/MinIO 41/41 和哈希一致。

### 2026-07-22 工作包三：非阻断集中整改

- N01—N04 由固定前端角色按既有页面规范与 034 登记范围完成：历史导入深链预置筛选、九个受影响页面查询区标准化、批量抽屉内容独立滚动与固定 footer、三组 FullFacts 稳定状态样本；6 个受影响测试文件共 96 项通过，类型检查通过。
- N06 通过 Element Plus 深入口导入和确定性依赖分包闭合；生产构建通过，最大 JavaScript chunk 为 441.13 KiB，不再出现大 chunk 或循环 chunk 告警。
- N10 将开发依赖链 `brace-expansion` 从 2.1.1 升级为 2.1.2；重新 `npm ci` 后完整 `npm audit` 与 `npm audit --omit=dev` 均为 0。
- N11 由固定后端角色补充通用 404 错误码与缺失资源异常映射；认证边界和认证后缺失资源均有回归测试，`ApiResponseTests` 10 项通过。
- N05 将 PowerShell 只读自动变量 `$PID` 替换为普通循环变量 `$processId`，并新增失败契约；`demo-data-self-test` 通过。
- N12/N13 的补偿控制已写入生产编排：只保留 Web 的 `127.0.0.1:45173`，API、PostgreSQL、MinIO 不发布宿主端口；生产基线自测与 Compose 配置检查通过，待冻结提交重建运行验证后关闭。
- 固定测试角色对 `64f33e664a2765e5bee824ae2b503c9d4955920e..0f7c32dc37bf3b2c8b0d984212985c86af560b21` 完成只读评审，报告为阻断 1、严重 1、一般 1，并明确禁止直接进入候选重建。
- 阻断修复真实执行时又暴露两项诊断事实：PowerShell 将接口 ISO 时间转为 `DateTime` 后丢失亚秒精度；034 旧 MinIO 镜像把 `_FILE` 默认成不可读的相对文件名。整改采用一秒内同一时刻比较，并只在 `_FILE` 可读时覆盖既有环境变量；重新执行后联合备份、元数据、FullFacts 211/211 和 41/41 全部通过。
- 严重问题最终采用 PostgreSQL/MinIO 文件型 secret 与 API 共享内存 configtree；一般问题改为迁移演练临时 ACL secret 文件。固定测试角色已对完整整改、API 启动失败安全和共享 tmpfs 架构完成三次差异复审，最终均为阻断 0、严重 0、一般 0。

### 2026-07-22 最终候选重建前置核验

- 密钥架构提交 `ef9f121866a9f435d3ba5ecba7e65127036b3621` 已构建为 `qherp/api:035-ef9f121866a9` 与 `qherp/web:035-ef9f121866a9`；API/Web OCI revision 精确指向该提交。
- PostgreSQL、MinIO、API、Web 四个业务容器健康，密钥保活容器运行；只有 Web 发布 `127.0.0.1:45173`，其余服务无宿主端口。
- 五容器 `Config.Env` 与四类真实密钥值匹配为 0；四个 API configtree 文件均为非空 `0600`、UID/GID `100:101`，API 以 `qherp` 身份只读挂载，保活服务无网络。
- 最终候选 API 单容器重启 17.085 秒恢复健康，内存密钥文件保持 4/4，代理健康 HTTP 200，验证了 B14 的自动恢复路径。
- 运行证据脚本对无 healthcheck 容器的严格模式兼容修复已按红测转绿，并由固定测试角色差异复审为 0/0/0；下一门禁是唯一全量验证窗口。

## 当前结论

035 已启动但尚未完成。路线合并和基线审计不代表上线准备、候选冻结、最终生产交付或第一版路线已经完成；只有 A01—A26 全部通过且阻断/严重问题为 0 后，才允许形成正式交付结论。
