# 验收标准

## 总则

阶段成果必须能在浏览器中实际访问和操作。仅代码完成、接口完成或文字说明不能作为验收完成依据。

自 2026-07-13 起，设计和开发仍需考虑移动端、窄屏和响应式适配；后续测试与验收只对本阶段实际新增或修改的相关页面进行桌面端浏览器视觉检验，不要求采集、保存或归档截图，不建立阶段视觉截图目录，也不测试或验收移动端、窄屏及响应式兼容性。本文中 000 至 020 已完成阶段的截图、移动端和视觉目录要求仅保留为历史验收事实，不再作为后续阶段要求；相关图片文件已于 2026-07-17 按用户要求清理，只保留测试计划、执行记录和视觉检查文字结论。

## 功能验收

- 满足任务文档中列出的功能范围。
- 正常流程和异常流程都有明确结果。
- 状态流转符合业务规则。

## 数据验收

- 数据新增、修改、删除或状态变更可追溯。
- 库存、数量、金额和成本数据口径一致。
- 关键业务数据不能依赖前端假数据。
- 成本归集一期只验收业务记录与来源追溯，不把业务金额或数量口径验收为正式财务核算结果。
- 成本记录必须能追溯到工单、产品、来源单据、记录人和记录时间。
- 采购入库过账必须增加库存余额、生成采购来源库存流水，并能从采购订单、采购入库和库存流水之间追溯。
- 采购单价只作为采购业务记录字段，不验收为应付、付款、税务、凭证或正式成本核算结果。
- 销售出库过账必须扣减库存余额、生成销售来源库存流水，并能从销售订单、销售出库和库存流水之间追溯。
- 销售来源库存流水必须回跳对应销售出库，不能误跳库存单据、采购入库或其他来源单据；缺少销售出库查看权限时应隐藏对应来源跳转。
- 销售单价只作为销售业务记录字段，不验收为应收、发票、收款、税务、凭证、收入确认或正式财务核算结果。
- 销售数量、出库数量和销售单价必须按 decimal 边界验收，避免浮点精度污染；零值、负数、超精度和非法格式按任务规格返回受控结果。
- 财务往来基础只验收业务台账、收付款登记、余额核销和来源追溯，不验收为总账、凭证、税务、完整发票、正式收入确认、正式成本结转或 BI 报表。
- 应收台账必须能追溯到已过账销售出库和销售订单；同一销售出库明细不得重复生成应收来源。
- 应付台账必须能追溯到已过账采购入库和采购订单；同一采购入库明细不得重复生成应付来源。
- 015 引入往来冲减后，应收金额必须等于已收金额、累计调整额与未收金额之和；应付金额必须等于已付金额、累计调整额与未付金额之和。不存在调整时累计调整额为零。
- 收款金额不得超过未收金额，付款金额不得超过未付金额；失败操作不得产生部分核销、错误余额或错误状态。
- 已过账收款和付款不可编辑、不可取消；已收清应收和已付清应付不可继续登记收付款。
- 经营报表基础只验收固定口径经营汇总、来源追溯、权限控制、空态异常态和相关页面视觉检验，不验收为可配置 BI、总账、凭证、正式财务报表、结账或税务结果。
- 经营报表汇总值必须能由来源明细解释；销售、采购、库存、生产、成本和往来报表必须明确业务口径。
- 经营报表接口不得写入或修改业务单据，不得绕过来源模块权限泄露来源详情。
- 退货退款与业务反冲基础只验收业务反向单据、库存反向流水、往来冲减、成本业务影响、经营报表净额和来源追溯，不验收为审批流、红字发票、税务、总账凭证、结账或银行退款支付。
- 已过账采购、销售、生产、库存、往来和成本原单不得被退货、退款或反冲直接改写或删除。
- 销售退货、采购退货、生产退料、生产补料和往来冲减必须能追溯原单、原行、反向单据、库存流水、往来记录、成本影响和经营报表来源。
- 退货数量不得超过可退数量，冲减金额不得超过可冲金额，反向出库不得造成负库存。
- 经营报表必须能区分原发生、反向发生和净额，不能只展示无法解释的净额。
- 业务期间与历史数据保护基础只验收按业务日期的锁定、解锁、审计和历史写入保护，不验收正式财务关账、会计期间、总账、凭证、税务、期间快照或成本结转。
- 锁定期间必须拒绝库存、采购、销售、生产、成本、往来和反向业务中影响经营口径的写入；查询、来源追溯和经营报表查看不得受阻。
- 开放期间可创建引用锁定期间历史原单的反向业务，反向影响必须按自身业务日期进入开放期间，不得回写锁定期间。
- 质量检验与库存质量状态基础只验收库存质量状态、轻量质量确认、库存可用量口径、非合格不可用、采购退货质量状态、权限、期间保护和来源追溯，不验收完整 QMS、SPC、批次、序列号、库位、MRP、审批流、正式财务或复杂返工返修。
- 历史库存必须默认迁移为合格库存；迁移后同一仓库、物料下各质量状态数量合计必须等于迁移前现存数量。
- 库存余额必须按仓库、物料和质量状态分层；库存流水必须记录质量状态；余额、流水、质量确认记录和来源单据之间的数量与状态必须一致。
- 017 阶段 `availableQuantity` 必须只代表合格可用库存，不得包含待检、不合格或冻结库存；018 起在库存余额和业务候选中进一步表示合格现货净可用，即扣除已占用和已预留库存，采购在途单列参考。
- 采购入库、生产完工入库、销售退货和生产退料默认形成待检库存，并生成可追溯的质量确认记录。
- 质量确认可以把待检库存转为合格、不合格或冻结，转换前后总库存不变，数量不平、超过待检余额、重复提交或状态非法必须被拒绝。
- 销售出库、生产领料和生产补料不能消耗非合格库存；总库存充足但合格库存不足时必须返回可识别的质量状态不可用或指定质量状态库存不足错误。
- 采购退货必须按显式质量状态退货，允许扣减待检、不合格或合格库存；冻结库存必须先解冻后才可退货。
- 质量确认、冻结、解冻、入库质量状态落账和按质量状态扣减必须受业务期间保护；锁定期间历史质量记录、库存余额、库存流水和来源追溯仍可查询。
- 质量状态相关失败事务不得产生部分写入，不能出现单据状态、库存余额、库存流水、质量记录或审计之间不一致。
- 质量状态库存必须能追溯来源单据、来源行、质量确认记录、状态转换流水和后续消耗记录。
- 库存可用量、占用、冻结与预留基础只验收库存可用量口径、轻量业务预留占用、物料级采购在途参考、仓库级现货可承诺量和计划净需求缺口前置口径，不验收完整 MRP、复杂 ATP、排产、批次、库位、库存月结或正式财务。
- 库存余额必须区分账面库存、合格现存、占用库存、预留库存、冻结库存、现货净可用、物料级采购在途参考、仓库级现货可承诺量和净需求缺口。
- `availableQuantity` 在 018 后必须代表现货净可用，不得包含待检、不合格、冻结、已占用或已预留库存。
- 销售订单行确认前必须填写预留仓库；确认必须按预留仓库生成 `SALES_ORDER + RESERVATION`；取消、关闭、数量减少和同仓库出库过账必须释放或消费对应预留；销售出库仓库必须与来源订单行预留仓库一致。
- 生产工单发布必须按 BOM 快照和领料仓库生成 `PRODUCTION_WORK_ORDER + RESERVATION`；不得把工单发布直接记为占用；取消、完成、用料减少和领料过账必须释放或消费对应预留。
- 采购订单确认或部分入库必须形成物料级采购在途参考；采购在途不按仓库计算，不进入仓库级账面库存或仓库级现货可承诺量；采购入库过账必须减少在途并形成具体仓库待检库存，质量确认后才进入合格现存。
- 冻结已占用或已预留的合格库存必须被拒绝；冻结库存不能进入可承诺量、占用、预留或普通消耗。
- 账面库存充足但现货净可用不足时，销售出库、生产领料、生产补料、采购退货或库存调减必须返回可识别的可用量不足或占用预留不可用错误。
- 销售确认门禁是选定预留仓库的现货净可用是否足够；现货不足但物料级采购在途足够时订单保持草稿并提示现货不足、在途仅供参考；本阶段不允许预留在途。
- 占用、预留、释放、消费和在途变化必须有来源追溯、审计记录和业务期间保护。
- `netRequirementShortageQuantity` 只输出非负缺口 `max(需求数量 - 可覆盖供给数量, 0)`，不生成 MRP 建议、计划订单或排产结果。
- 批次、序列号与来源去向追溯基础只验收核心单据链路的追踪身份采集、库存维度校验、质量冻结联动和基础来源去向查询，不验收完整 WMS、扫码、库位、复杂召回、MES、MRP 或正式财务。
- 物料必须支持不追踪、批次追踪和序列号追踪三种追踪方式；已有库存、库存流水、占用预留或业务引用的物料不得随意切换追踪方式。
- 批次追踪入库允许同一业务行拆分多个批次，追踪分配数量合计必须等于业务数量；序列号追踪入库必须按单件序列落账，序列数量必须等于业务数量，重复序列必须被拒绝。
- 采购入库和生产完工入库必须生成可追踪的批次或序列库存、库存流水和质量确认记录；数量不一致、缺少追踪身份、非法追踪方式或序列重复不得产生部分写入。
- 销售出库、生产领料、采购退货和生产补料只能选择后端返回的可用追踪候选；待检、不合格、冻结、已占用、已预留、跨仓或超可用候选必须禁用或被后端拒绝。
- 销售退货必须继承原销售出库追踪身份，生产退料必须继承原生产领料追踪身份；来源追踪身份应只读回显，不允许用户改选或篡改。
- 质量确认、冻结和解冻必须保留原批次或序列身份；同一批次拆分不同质量结果、不同序列确认不同质量结果时，库存余额、流水和追溯链路必须保持一致。
- 库存余额、库存流水和占用预留必须按仓库、物料、质量状态和追踪身份维度可查；批次/序列筛选、来源单据、去向单据、当前结存和质量事件必须能相互解释。
- 批次追溯和序列追溯必须返回来源、当前库存、质量事件、出库去向、退货反向和库存流水；无来源详情权限时只能返回受限摘要，不得泄露越权详情。
- 追踪库存写入必须受业务期间保护；锁定期间内影响库存事实、质量状态、冻结解冻、退货继承或追踪分配的写入必须被拒绝，历史查询和追溯仍可访问。
- 销售项目、合同、单位换算、编码规则、BOM 版本/ECO/替代料、固定审批与文档平台、库存计价和仓储账实一致的详细验收，以 020 至 023 对应唯一权威任务说明为准。
- BOM 必须按业务日期唯一命中已发布有效版本；当前有效、未来生效和历史失效必须可区分，生产工单发布按计划开工日期重新校验并固化快照。
- 固定审批状态与业务状态分离；提交人不得自批，任务、消息、附件快照、固定打印和文档任务同时受本人关系、业务对象权限、版本、幂等和审计保护。
- 物料导入只新增，BOM 导入只创建或更新一个草稿；导入必须全文件校验、暂存、确认和原子提交，失败不得形成半批业务数据。
- 公共库存按单公司+物料移动加权平均，项目库存按项目+物料+真实来源成本层；库存余额、数量流水、公共池、项目层和价值流水必须相互解释。
- 历史未估值库存必须保持空金额和明确状态，完成期初估值前不得价值型出库、盘亏或所有权转换；不得静默写为零成本。
- 仓库调拨必须形成成对数量/价值流水且企业总价值不变；所有权转换、盘差和估值调整必须按固定审批与业务期间原子过账。
- 盘点必须区分未盘、实盘 `0` 和非零实盘，开始后固化账面快照和范围锁；分页、多行、陈旧版本和审批失败不得造成部分写入或锁泄漏。

## 权限验收

- 菜单、按钮、字段和数据范围符合角色权限规则。
- 无权限操作应给出明确反馈。
- 业务期间的查看、创建、维护、锁定和解锁使用独立权限；无权限用户不得通过页面、路由或接口锁定、解锁或绕过写入保护。
- 质量确认查看、质量确认处理、冻结和解冻必须使用独立权限；未登录、无权限和只读用户不得通过菜单、路由、按钮或接口绕过质量状态控制。
- 前端隐藏或禁用质量操作不能替代后端鉴权；越权调用质量确认、冻结、解冻或非合格消耗接口必须被后端拒绝。
- 库存可用量查看和占用预留查看必须使用明确权限；只读用户可查看库存口径和追溯，不得执行影响占用、预留、释放、消费或在途口径的写动作。
- 销售、生产、采购模块触发的预留、占用、释放和在途变化必须由对应模块写权限和后端接口鉴权共同约束。
- 批次、序列号和追溯查看必须使用明确权限；无追溯权限用户不得通过页面、路由或接口获取来源去向详情。
- 采购、销售、生产、质量和库存模块中的追踪分配、候选选择、来源继承、质量确认、冻结解冻写动作必须由对应模块写权限和后端接口鉴权共同约束。
- 前端隐藏、禁用或只读展示追踪控件不能替代后端鉴权；越权提交追踪分配、来源继承、质量确认、冻结解冻或追溯查询必须被后端拒绝或脱敏。
- 客户/供应商结算税务资料、合同摘要、审批附件、打印结果和文档任务下载必须继承业务对象权限，猜测 ID 或撤销权限后不得继续访问。
- `inventory:valuation:view` 必须由后端统一控制单价、金额、平均价、成本层金额和相关导出；无权限用户仍可按原权限查看数量，但所有接口、页面、审批摘要和来源跳转不得泄露成本。

## UI 验收

- `docs/ui/page-standards.md` 是页面结构、导航、标题、搜索栏、表格、表单、候选池、弹窗、抽屉、操作和报表的强制验收基线；每个阶段必须逐页核对适用检查项。
- 页面规范验收记录必须列明新增、修改和受影响页面，以及相关检查项的满足、未满足、已整改和经用户确认的例外；不得只写笼统“视觉通过”。
- 页面能打开、核心动作能执行、控制台无错误或没有明显页面级溢出，只能证明对应功能或冒烟路径通过，不能替代 `page-standards.md` 合规核对。
- 异常、加载、空状态和成功反馈完整。
- 浏览器验收阶段只对本阶段实际新增或修改的相关页面、组件和受影响业务状态进行桌面端视觉检验。
- 视觉检验基于真实运行页面完成，默认不采集、不保存截图，也不建立 `visual-audit` 目录；验收通知只需说明检查范围、发现问题、处理结果和结论。
- 视觉检验必须覆盖桌面端布局稳定性、信息密度、关键操作可见性、文案溢出、控件重叠和业务状态识别。
- 如视觉问题影响核心操作、关键数据识别、权限状态判断或明显降低工作台扫描效率，必须修复并复验受影响桌面端路径后才允许进入阶段验收。
- 已交付页面如发现违反 `page-standards.md`，必须登记页面、状态、差异和处理计划；未完成专项核对与整改前不得把历史视觉冒烟解释为规范已经闭合。
- 以下 016 至 020 阶段专项条目保留当时真实验收要求，仅作历史记录，不适用于后续阶段。
- 业务期间阶段视觉分析覆盖期间列表、锁定确认、写入失败、只读权限、无权限和 390px 窄屏。
- 质量状态阶段视觉分析覆盖质量确认列表、质量确认处理抽屉、库存余额质量状态分布、销售出库候选、生产领料候选、采购退货候选、期间保护拦截、权限差异、空态、异常态和 390px 窄屏。
- 质量状态页面必须同时展示总现存、合格可用、待检、不合格和冻结数量；质量状态不得只依靠颜色表达，必须有中文文案。
- 质量确认处理抽屉、候选库存区域和库存余额宽表在桌面和 390px 窄屏下不得遮挡关键字段、错误提示、底部操作或质量状态禁用原因。
- 018 阶段视觉分析覆盖库存余额可用量总览、占用预留追溯、物料级采购在途参考、销售订单行预留仓库、销售出库候选、生产领料候选、冻结拦截、期间保护、权限差异、空态、异常态和 390px 窄屏。
- 库存余额页面必须清晰展示账面库存、合格现存、占用库存、预留库存、冻结库存、现货净可用、物料级采购在途参考、仓库级现货可承诺量和净需求缺口；采购在途不得被表现为现货可用或销售确认门禁。
- 销售和生产候选库存区域必须展示本次最多出库或本次最多领料、禁用原因和后端返回的可用量口径，不能只显示笼统“库存不足”。
- 019 阶段历史视觉分析目录为 `docs/testing/019-batch-serial-trace-visual-audit/`，当时覆盖测试计划中的 19 张桌面和移动端截图；该截图与移动端清单不适用于后续阶段。
- 追踪分配编辑器、出库候选选择器、退货来源继承、质量确认、冻结错误、库存余额、库存流水和追溯抽屉必须清晰展示批次/序列身份、数量、质量状态、可用量、禁用原因和来源去向。
- 长批次号、长序列号、候选禁用原因和追溯节点不得遮挡核心操作、错误提示、抽屉底部按钮或移动端选择器操作。
- 已过账、已取消、只读和无权限状态下，追踪身份必须可识别且不可编辑；权限受限的追溯抽屉必须明确显示受限状态。

## 019 历史验证记录

- 019 WP1-5 已在 `codex/019-batch-serial-trace` 分支提交：WP1-3 后端提交 `aa02311`、`1d9aa16`、`4a79b9a`；WP4 通用前端组件和库存页面位于 `aa02311`；WP5 前端业务页面提交 `4316e8d`。
- WP5 整改后前端定向测试已通过：`npm --prefix apps/web test -- procurementApi.spec.ts salesApi.spec.ts productionApi.spec.ts returnRefundReversalApi.spec.ts qualityInventoryStatusApi.spec.ts inventoryApi.spec.ts PurchaseReceiptFormView.spec.ts SalesShipmentFormView.spec.ts ProductionExecutionForms.spec.ts SalesReturnViews.spec.ts PurchaseReturnViews.spec.ts ProductionMaterialReversalViews.spec.ts QualityInspectionViews.spec.ts PurchaseReceiptDetailView.spec.ts SalesShipmentDetailView.spec.ts`，15 文件、155 个测试通过。
- WP5 整改后前端类型检查已通过：`npm --prefix apps/web run typecheck`。
- WP5 整改后后端 JDK 21 定向验证已通过：`./mvnw.cmd '-Dtest=ReversalAdminControllerTests,QualityAdminControllerTests,ProcurementAdminControllerTests,ProductionAdminControllerTests' test`，90 个测试通过；`./mvnw.cmd '-Dtest=QualityAdminControllerTests,ProductionAdminControllerTests' test`，38 个测试通过。
- WP5 整改后 `git diff --check` 退出码 0，仅换行提示；UI、产品、测试、前后端交叉复审均已批准 WP5。
- WP6 浏览器前置诊断已执行：前端 `http://127.0.0.1:5173/` 返回 HTTP 200，后端 `http://127.0.0.1:18080/api/health` 返回 `{"service":"qherp-api","status":"UP"}`，`qherp-postgres` 运行且健康；受保护路由登录墙和本地管理员测试账号登录均已核对。
- WP6 只读数据库探测确认当前本地 `qherp` 数据库已应用 V16 `batch serial trace schema`；`inv_batch`、`inv_serial`、`inv_stock_tracking_allocation` 表和 `mst_material.tracking_method` 列均存在。
- 019 WP6 浏览器主路径、异常路径、权限路径、只读路径、期间锁定路径、移动端路径和当时要求的 19 张业务视觉截图已完成，记录位于 `docs/testing/019-batch-serial-trace-visual-audit/notes.md`；产品/UI 复审、交付前全量验证、主分支合入、推送和验收服务均已完成。本节只保留历史证据，不代表当前仍要求截图或移动端验收。

## 阻断条件

- 无法本地启动或部署。
- 核心页面无法浏览器访问。
- 关键业务数据错误。
- 权限绕过。
- 本阶段相关页面改动未完成真实桌面端浏览器视觉检验，或视觉问题影响核心操作、关键数据识别、权限状态判断。
- 库存、工单、成本相关流程不可追溯。
- 采购订单、采购入库和采购来源库存流水不可追溯。
- 销售订单、销售出库和销售来源库存流水不可追溯。
- 销售出库库存扣减错误、产生负库存，或重复过账污染库存和流水。
- 页面或接口把销售单价作为应收、发票、收款、税务、收入确认、凭证或正式财务结果交付。
- 应收、收款、应付、付款任一财务往来主路径无法完成。
- 应收或应付来源追溯断链、重复来源未阻断、金额余额错误或状态和金额矛盾。
- 超额收付款被接受，或收付款失败后产生脏金额、半提交核销或错误状态。
- 页面或接口把财务往来作为总账、凭证、税务、完整发票、正式收入确认、正式成本结转或 BI 报表交付。
- 经营报表核心页面无法访问、关键报表接口不可用，或汇总值与来源明细对不上。
- 经营报表来源追溯不可用、追溯到错误单据，或无权限用户可查看报表或来源详情。
- 页面或接口把经营报表作为可配置 BI、总账、凭证、结账、正式财务报表或税务结果交付。
- 退货退款与业务反冲核心页面无法访问、关键接口不可用，或数据库 V11 迁移失败。
- 反向业务直接改写或删除已过账原单。
- 系统接受超退、超冲、重复反冲，或反向出库造成负库存。
- 库存余额、往来余额、成本净额或经营报表净额与反向业务来源不一致。
- 无权限用户可访问退货退款反冲接口，或来源受限响应泄露单号、主键、数量、金额、业务日期、状态或路由参数。
- 数据库迁移失败或初始化数据不可用。
- 账号权限模块中未登录、无权限或停用账号仍可访问受保护资源。
- 阻断缺陷未修复或未回归。
- 锁定期间仍允许影响经营口径写入、跨期反向业务回写历史期间、期间锁定或解锁权限绕过、锁定期间查询受阻，均为阻断缺陷。
- 质量状态数据库迁移失败、历史库存未默认迁移为合格库存、迁移后库存总量不一致，均为阻断缺陷。
- 库存余额、库存流水、质量确认记录、来源单据之间数量或质量状态不一致，均为阻断缺陷。
- `availableQuantity` 包含待检、不合格或冻结库存，或页面把总库存误展示为可销售、可领用、可补料数量，均为阻断缺陷。
- 采购入库、生产完工入库、销售退货或生产退料未默认形成待检库存，或未生成可追溯质量确认记录，均为阻断缺陷。
- 质量确认转换前后总库存变化，数量不平、超过待检余额、重复提交或状态非法仍被接受，均为阻断缺陷。
- 销售出库、生产领料或生产补料成功消耗待检、不合格或冻结库存，均为阻断缺陷。
- 采购退货未按显式质量状态扣减，或直接扣减冻结库存，均为阻断缺陷。
- 无权限、只读或未登录用户能处理质量确认、冻结、解冻，或通过接口绕过质量状态校验，均为阻断缺陷。
- 锁定期间仍允许质量确认、冻结、解冻、入库质量状态落账或按质量状态扣减，均为阻断缺陷；锁定期间历史质量记录、库存余额、库存流水或来源追溯无法查询，也按阻断缺陷处理。
- 质量状态失败事务产生单据状态、库存余额、库存流水、质量记录或审计的部分写入，均为阻断缺陷。
- 质量状态来源追溯无法关联来源单据、来源行、质量确认记录、状态转换流水或后续消耗，均为阻断缺陷。
- 018 库存可用量公式错误、数量重复计算、`availableQuantity` 包含非合格或已占用预留库存，均为阻断缺陷。
- 销售订单、生产工单、销售出库、生产领料、采购入库、采购退货或生产补料绕过占用预留校验导致超卖、超领、负库存或残留占用，均为阻断缺陷。
- 销售订单行缺少预留仓库仍可确认、销售出库仓库与来源订单行预留仓库不一致仍可过账、或系统自动跨仓释放改仓分配，均为阻断缺陷。
- 生产工单发布直接生成占用而不是预留，均为阻断缺陷。
- 采购在途被计入账面库存、立即可用库存或仓库级现货可承诺量，均为阻断缺陷。
- 现货不足但以物料级采购在途为依据确认销售订单或预留在途，均为阻断缺陷。
- 已占用或已预留库存仍可被冻结、承诺、出库、领料或补料，均为阻断缺陷。
- 占用预留来源重复、释放后仍残留、过账后未消费或失败事务产生部分写入，均为阻断缺陷。
- 锁定期间仍允许影响占用、预留、释放、消费或采购在途的写入，或历史占用预留和在途追溯无法查询，均为阻断缺陷。
- 库存余额、销售候选、生产候选或采购在途页面缺失 018 视觉分析证据，或视觉问题影响关键数量口径识别，均为阻断缺陷。
- 019 追踪模型数据库迁移失败、存量库存追踪维度迁移错误、物料追踪方式约束失效，均为阻断缺陷。
- 采购入库或生产完工入库追踪分配数量与业务数量不一致仍可提交、缺少追踪身份仍可提交、序列号重复仍可入库，均为阻断缺陷。
- 销售出库、生产领料、采购退货或生产补料可选择不可用批次/序列、跨仓追踪库存、冻结库存、已占用已预留库存或超可用数量，均为阻断缺陷。
- 销售退货或生产退料未继承来源追踪身份、继承身份可被用户篡改、提交缺少 `sourceAllocationId` 或来源身份串链，均为阻断缺陷。
- 质量确认、冻结或解冻后批次/序列身份丢失、质量状态与库存余额或库存流水不一致，均为阻断缺陷。
- 批次追溯或序列追溯无法关联来源、当前库存、质量事件、去向、反向业务和流水，或无权限响应泄露越权来源详情，均为阻断缺陷。
- 锁定期间仍允许追踪库存写入、追踪分配、质量确认、冻结解冻或来源继承写入，或锁定期间历史追溯无法查询，均为阻断缺陷。
- 019 交付时若缺失当时要求的视觉证据，或追踪分配、候选选择、来源继承、质量处理、追溯抽屉和权限状态影响关键操作，应按阻断处理；019 已完成。后续阶段不复用其截图和移动端门禁。
- 021 后同一父项同一业务日期命中多个已发布 BOM、工单按错误日期选择 BOM、已发布工单快照被后续版本/ECO 改写，均为阻断缺陷。
- 固定审批可自批、越权查看/处理待办，或消息、附件、打印、导入导出成为业务权限旁路，均为阻断缺陷。
- 导入绕过业务服务、校验失败后部分写入，文档任务假完成、结果对象不存在或撤权后仍可下载，均为阻断缺陷。
- 公共池、项目成本层、库存余额、数量流水或价值流水不一致，出现负数量/金额、项目层串项目、调拨改变企业总价值，均为阻断缺陷。
- 历史未估值库存被当作零成本、盘点快照可变、未盘与零混淆、范围锁泄漏、盘差半提交，均为阻断缺陷。
- 无库存价值权限用户从任一页面、接口、审批、附件、导出或来源追溯获得成本金额，均为阻断缺陷。

## 账号权限首期验收

- 用户可登录、退出，登录失败有明确提示。
- 未登录访问受保护页面和接口会被拦截。
- 停用账号不能登录。
- 不同角色看到的菜单和可执行操作不同。
- 前端隐藏不能替代后端接口鉴权，越权接口请求必须被拒绝。
- 权限变更后的生效规则与文档一致。
